<!------------------------------------ html ---------------------------------------->


    <!-- first step start -->

<!--------------------- answer naumber 1 start ----------------->

<!-- HTML (HyperText Markup Language) হল ওয়েব পেজ তৈরির মূল ভাষা যা ওয়েব ব্রাউজারে কন্টেন্ট প্রদর্শন করার জন্য ব্যবহৃত হয়। এটি ওয়েবসাইটের গঠন তৈরি করে এবং টেক্সট, ইমেজ, লিঙ্ক, ফর্ম এবং বিভিন্ন মাল্টিমিডিয়া উপাদানগুলো ওয়েব পেজে কীভাবে সাজানো হবে তা নির্দেশ করে। -->


<!-- 
HTML এর উদ্দেশ্য:

1: ওয়েব কন্টেন্ট প্রদর্শন: HTML ওয়েবসাইটে থাকা বিভিন্ন টেক্সট, ছবি, ভিডিও প্রভৃতি উপাদানকে প্রদর্শন করে।
2: স্ট্রাকচার তৈরি করা: ওয়েব পেজের গঠন তৈরি করতে সহায়তা করে যাতে পেজটি সহজে পড়া এবং বোঝা যায়।
3-Hyperlink ব্যবহার করে পেজ সংযোগ: এক পেজ থেকে অন্য পেজে সহজে যাওয়ার জন্য লিঙ্ক তৈরি করতে দেয়। -->

<!-- 
HTML এর মৌলিক সিনট্যাক্স
HTML কোড লেখা হয় ট্যাগ এবং এলিমেন্ট এর মাধ্যমে। প্রতিটি HTML ট্যাগব্র্যাকেটে ঘেরা থাকে এবং সাধারণত ওপেনিং এবং ক্লোজিং ট্যাগ থাকে। উদাহরণস্বরূপ: -->

<!DOCTYPE html>
<html>
<head>
    <title>Page Title</title>
</head>
<body>
    <h1>This is a Heading</h1>
    <p>This is a paragraph.</p>
    <a href="https://example.com">This is a link</a>
</body>
</html>
```

 <!-- এই উদাহরণে ব্যবহৃত মৌলিক ট্যাগগুলোর ব্যাখ্যা: -->
<!DOCTYPE html>
 <!-- HTML ডকুমেন্টের টাইপ নির্দেশ করে। এটি HTML5 ব্যবহার করা হচ্ছে তা জানায়। -->


<html>
<!-- পুরো HTML ডকুমেন্টের জন্য রুট এলিমেন্ট। -->
 
<head>
 <!-- ডকুমেন্ট সম্পর্কে তথ্য ধারণ করে, যেমন -->


<title>
    <!-- স্টাইল, মেটাডেটা। -->


<title>
 <!-- ব্রাউজারের ট্যাবে দেখানো শিরোনাম। -->


<body>
 <!-- ডকুমেন্টের দৃশ্যমান অংশ যা ব্রাউজারে দেখানো হয়। -->


<h1>
<!-- এটি একটি শিরোনাম ট্যাগ, -->
   
<h1> থেকে <h6>
 <!-- পর্যন্ত বিভিন্ন স্তরের শিরোনাম তৈরি করতে ব্যবহার করা হয়। -->


<p>
<!-- এটি একটি প্যারাগ্রাফ ট্যাগ যা টেক্সট প্রদর্শনের জন্য ব্যবহৃত হয়। -->


<a>
 <!-- এটি একটি লিঙ্ক তৈরি করে, যেখানে `href` অ্যাট্রিবিউটটি লিঙ্কের গন্তব্য নির্দেশ করে।  -->

<!-- HTML সহজেই শিখা যায় এবং ওয়েব ডেভেলপমেন্টের জন্য অন্যতম মূল বিষয়। -->

<!--------------------- answer naumber 1 end ----------------->



 <!--------------------- answer number 2 start ----------------->

    <!-- 
    HTML Tags কী:
    HTML ট্যাগ হল এমন কিছু নির্দেশনা যা ব্রাউজারকে বলে কীভাবে ওয়েব পেজে বিষয়বস্তু প্রদর্শন করতে হবে। প্রতিটি HTML ট্যাগ `<` দিয়ে শুরু হয় এবং `>` দিয়ে শেষ হয়। সাধারণত একটি ওপেনিং ট্যাগ এবং ক্লোজিং ট্যাগ থাকে, যেখানে ক্লোজিং ট্যাগটির আগে `/` থাকে। উদাহরণ: <tagname>Content</tagname>
    -->

    <!-- HTML ট্যাগের গঠন:
    1. প্রতিটি ট্যাগ `<` দিয়ে শুরু এবং `>` দিয়ে শেষ হয়।
    2. ওপেনিং ট্যাগের পরে ক্লোজিং ট্যাগ আসে।
    3. ক্লোজিং ট্যাগটি `/` চিহ্ন দিয়ে শুরু হয়, যেমন: `</tagname>`.
    -->

    <!-- উদাহরণ স্বরূপ ৫টি HTML ট্যাগ: -->

    <h1> <!-- এটি একটি শিরোনাম ট্যাগ, সাধারণত বড় শিরোনামের জন্য ব্যবহার করা হয়। --> </h1>

    <p> <!-- এটি একটি প্যারাগ্রাফ ট্যাগ যা সাধারণ টেক্সট প্রদর্শনের জন্য ব্যবহৃত হয়। --> </p>

    <a href="#"> <!-- এটি একটি অ্যাঙ্কর (লিংক) ট্যাগ, যা লিংক তৈরি করতে ব্যবহৃত হয়। `href` এর মাধ্যমে লিঙ্কের গন্তব্য নির্ধারণ করা হয়। --> </a>

    <img src="image.jpg" alt="Image"> <!-- এটি একটি ইমেজ ট্যাগ, যা ওয়েব পেজে ছবি প্রদর্শনের জন্য ব্যবহৃত হয়। `src` অ্যাট্রিবিউটের মাধ্যমে ইমেজের লিংক এবং `alt` এর মাধ্যমে বর্ণনা নির্ধারণ করা হয়। -->

    <ul>
        <li>Item 1</li>
        <li>Item 2</li>
    </ul>
    <!-- <ul> এবং <li> ট্যাগগুলি তালিকা তৈরি করতে ব্যবহৃত হয়। <ul> অসম তালিকা এবং <li> তালিকার আইটেম নির্দেশ করে। -->

    <!--------------------- answer number 2 end ----------------->



   <!--------------------- answer number 3 start ----------------->

    <!-- 
    HTML, Head, Body এবং <!DOCTYPE html> এর ভূমিকা এবং পার্থক্য:
    
    1. <!DOCTYPE html> ঘোষণা:
       - এটি একটি ঘোষণা যা ব্রাউজারকে জানায় যে এটি একটি HTML5 ডকুমেন্ট।
       - এটি ডকুমেন্টের শীর্ষে রাখা হয় এবং সঠিকভাবে HTML রেন্ডার করতে ব্রাউজারকে সহায়তা করে।
       - এটি HTML ডকুমেন্টের কোনো ট্যাগ নয়, বরং HTML5-এর জন্য একটি নির্দেশনা।
    -->

    <!DOCTYPE html>

    <!-- 
    2. <html> ট্যাগ:
       - HTML ডকুমেন্টের মূল বা রুট এলিমেন্ট।
       - এটি ডকুমেন্টের শুরু এবং শেষ নির্দেশ করে এবং এর ভিতরে <head> এবং <body> সহ সকল HTML কন্টেন্ট রাখা হয়।
    -->

    <html>

    <!-- 
    3. <head> ট্যাগ:
       - <head> ট্যাগের মধ্যে ডকুমেন্টের মেটাডেটা রাখা হয় যা সরাসরি ব্রাউজারে দৃশ্যমান নয়।
       - এর মধ্যে শিরোনাম (<title>), স্টাইল, স্ক্রিপ্ট, এবং মেটা ট্যাগ থাকে, যা SEO ও পেজ লোডিং অপটিমাইজ করতে সহায়ক।
    -->

    <head>
        <title>Page Title</title>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="styles.css">
    </head>

    <!-- 
    4. <body> ট্যাগ:
       - <body> ট্যাগের মধ্যে থাকে পেজের দৃশ্যমান কন্টেন্ট যা ব্রাউজারে প্রদর্শিত হয়।
       - এর ভিতরে শিরোনাম, প্যারাগ্রাফ, ইমেজ, লিঙ্ক, এবং অন্যান্য কন্টেন্ট এলিমেন্ট থাকে।
    -->

    <body>
        <h1>This is a Heading</h1>
        <p>This is a paragraph.</p>
    </body>

    <!--------------------- answer number 3 end ----------------->


    <!-- aswer 4 -->
    <!-- 
    <title> ট্যাগের কাজ:
    - <title> ট্যাগটি ওয়েব পৃষ্ঠার শিরোনাম নির্ধারণ করে, যা ব্রাউজারের ট্যাব বা শিরোনাম বার-এ প্রদর্শিত হয়।
    - বুকমার্ক করলে, <title> ট্যাগের বিষয়বস্তুটি শিরোনাম হিসেবে ব্যবহৃত হয়।
    - SEO এর জন্য, <title> গুরুত্বপূর্ণ কারণ সার্চ ইঞ্জিন এটি সার্চ রেজাল্টে শিরোনাম হিসেবে প্রদর্শন করে।
    -->

    <title>আমার ওয়েব পৃষ্ঠার শিরোনাম</title> <!-- ব্রাউজারের ট্যাব বা শিরোনাম বার-এ প্রদর্শিত টাইটেল -->

</head>
<body>

    <h1>আমার ওয়েব পৃষ্ঠায় স্বাগতম</h1>
    <p>এই পৃষ্ঠাটি <title> ট্যাগ ব্যবহারের একটি উদাহরণ।</p>

    
        <!-- aswer 5 -->
    <!--
    কমেন্ট কীভাবে যোগ করা হয়:
    HTML এ কমেন্ট যোগ করতে `< !--` দিয়ে শুরু করে এবং `-->` দিয়ে শেষ করতে হয়।
    উদাহরণ:
    <!-- এই অংশটি ব্রাউজারে প্রদর্শিত হবে না -->

    <title>কমেন্ট যুক্ত করা শিখুন</title>
</head>
<body>

    <h1>কমেন্ট এর উদাহরণ</h1>

    <!--
    কমেন্ট কেন প্রয়োজনীয়:
    ১. কোড ব্যাখ্যা করা: কোডের কোন অংশ কেন লেখা হয়েছে তা ব্যাখ্যা করতে।
    ২. ভবিষ্যতে পরিবর্তন বা উন্নয়নের জন্য নির্দেশনা দিতে।
    ৩. টিমের অন্যান্য ডেভেলপারদের জন্য কোডের মধ্যে স্পষ্টতা আনা।
    -->

    <p>এই পৃষ্ঠাটি HTML এ কমেন্ট ব্যবহারের একটি উদাহরণ।</p>

    <!-- কমেন্ট করার উদাহরণ -->
    <p>এটি একটি সাধারণ প্যারা, যা ব্রাউজারে দেখা যাবে।</p>



    <!-- first step end  -->


    <!-- second step start -->


    <!--------------------- Answer Number 6 Start --------------------->

<!--
HTML উপাদান (element) এবং HTML অ্যাট্রিবিউট (attribute) এর মধ্যে পার্থক্য:

HTML উপাদান:
HTML উপাদান হল একটি ট্যাগ এবং তার ভিতরে থাকা কন্টেন্ট। এটি একটি শুরু এবং একটি শেষ ট্যাগ নিয়ে গঠিত হতে পারে। উদাহরণস্বরূপ:-->

<p>This is a paragraph.</p>

<!--এখানে <p> হল HTML উপাদান এবং "This is a paragraph." হল এর কন্টেন্ট।

HTML অ্যাট্রিবিউট:
HTML অ্যাট্রিবিউট হল একটি উপাদানের বৈশিষ্ট্য যা ট্যাগের মধ্যে নির্দিষ্ট করা হয়। এটি উপাদানটির আচরণ বা কন্টেন্ট সম্পর্কে অতিরিক্ত তথ্য প্রদান করে। উদাহরণস্বরূপ:-->

<a href="https://example.com">This is a link</a>

<!--এখানে href একটি অ্যাট্রিবিউট যা <a> (লিঙ্ক) উপাদানকে একটি নির্দিষ্ট URL নির্দেশ করে। "https://example.com" হল অ্যাট্রিবিউটের মান।

সারসংক্ষেপ:
- HTML উপাদান: ট্যাগ এবং তার কন্টেন্ট।
- HTML অ্যাট্রিবিউট: উপাদানের অতিরিক্ত তথ্য বা বৈশিষ্ট্য।

-->

<!--------------------- Answer Number 6 End --------------------->


<!--------------------- Answer Number 7 Start --------------------->

<!--
HTML এ কিছু গ্লোবাল অ্যাট্রিবিউট রয়েছে যা বিভিন্ন HTML উপাদানে ব্যবহার করা যেতে পারে। প্রধান গ্লোবাল অ্যাট্রিবিউটগুলি হল:

1. **class**:
   - এটি একটি বা একাধিক ক্লাস নাম উল্লেখ করে যা CSS স্টাইলিং বা জাভাস্ক্রিপ্ট কার্যকলাপের জন্য ব্যবহৃত হয়।
   - উদাহরণ:
     <div class="my-class">This is a div with a class.</div>

2. **id**:
   - এটি একটি অনন্য পরিচিতি প্রদান করে যা একটি নির্দিষ্ট উপাদানকে চিহ্নিত করে। এটি একবারই ব্যবহৃত হয় একটি HTML ডকুমেন্টে।
   - উদাহরণ:
     <h1 id="header">This is a header with an ID.</h1>

3. **style**:
   - এটি ইনলাইন CSS স্টাইল নির্দেশ করতে ব্যবহৃত হয়।
   - উদাহরণ:
     <p style="color: red;">This text is red.</p>

4. **title**:
   - এটি একটি সংক্ষিপ্ত বিবরণ প্রদান করে যা উপাদানের উপর মাউস রাখলে প্রদর্শিত হয়।
   - উদাহরণ:
     <img src="image.jpg" title="This is an image." alt="Image Description">

সারসংক্ষেপ:
- **class**: CSS এর জন্য ক্লাস নাম।
- **id**: অনন্য পরিচিতি।
- **style**: ইনলাইন CSS।
- **title**: উপাদানের সম্পর্কে তথ্য।

-->

<!--------------------- Answer Number 7 End --------------------->


<!--------------------- Answer Number 8 Start --------------------->

<!--
`alt` অ্যাট্রিবিউটটি একটি ইমেজের জন্য বিকল্প টেক্সট প্রদান করে যা ইমেজটি লোড না হলে বা যদি ব্যবহারকারী ইমেজটি দেখতে না পারে তখন প্রদর্শিত হয়। এর মূল উদ্দেশ্য হল:

1. **অ্যাক্সেসিবিলিটি**:
   - ভিজ্যুয়ালি চ্যালেঞ্জড ব্যবহারকারীদের জন্য স্ক্রীন রিডার দ্বারা টেক্সট পড়া সম্ভব করে।

2. **SEO (সার্চ ইঞ্জিন অপটিমাইজেশন)**:
   - সার্চ ইঞ্জিনগুলিকে ইমেজের কনটেন্ট বুঝতে সহায়তা করে।

3. **ব্যবহারকারীর অভিজ্ঞতা**:
   - যদি ইমেজটি লোড না হয়, তবে ব্যবহারকারী একটি টেক্সট বর্ণনা দেখতে পায় যা ইমেজের বিষয়বস্তু নির্দেশ করে।

উদাহরণ:
<img src="image.jpg" alt="A beautiful sunset over the mountains." />

এই উদাহরণে, যদি `image.jpg` লোড না হয়, তবে ব্যবহারকারী "A beautiful sunset over the mountains." টেক্সটটি দেখতে পাবে।

-->

<!--------------------- Answer Number 8 End --------------------->


<!--------------------- Answer Number 9 Start --------------------->

<!--
`href` (Hypertext Reference) অ্যাট্রিবিউটটি `<a>` ট্যাগের একটি গুরুত্বপূর্ণ অংশ যা একটি লিঙ্কের গন্তব্য নির্ধারণ করে। এটি লিঙ্কের URL (ইউনিফর্ম রিসোর্স লোকেটর) নির্দেশ করে এবং ব্যবহারকারী যখন লিঙ্কে ক্লিক করে তখন ব্রাউজার সেই URL-এ নিয়ে যায়।

`href` অ্যাট্রিবিউটের ভূমিকা:

1. **লিঙ্কের গন্তব্য নির্ধারণ**:
   - লিঙ্কের ক্লিকের মাধ্যমে যে পৃষ্ঠায় নিয়ে যাবে তা নির্দেশ করে।

2. **ভিন্ন প্রকারের লিঙ্ক**:
   - এটি অভ্যন্তরীণ (একই ডোমেইনের) বা বাইরের (অন্য ডোমেইনের) লিঙ্ক হতে পারে।

3. **ইমেইল বা ফাইল ডাউনলোড**:
   - `mailto:` ব্যবহার করে ইমেইল পাঠানোর জন্য বা ফাইল ডাউনলোডের জন্যও ব্যবহার করা যেতে পারে।

উদাহরণ:
<a href="https://example.com">Visit Example.com</a>

এই উদাহরণে, ব্যবহারকারী "Visit Example.com" লিঙ্কে ক্লিক করলে ব্রাউজার https://example.com এ নিয়ে যাবে।

-->

<!--------------------- Answer Number 9 End --------------------->


<!--------------------- Answer Number 10 Start --------------------->

<!--
`target` অ্যাট্রিবিউটটি `<a>` ট্যাগের একটি অংশ যা লিঙ্কটি ক্লিক করার পরে কিভাবে খোলা হবে তা নির্ধারণ করে। এটি বিভিন্ন মান গ্রহণ করতে পারে, যার মধ্যে সবচেয়ে সাধারণগুলি হলো:

1. **_self**: (ডিফল্ট) লিঙ্কটি একই উইন্ডো বা ট্যাবে খোলে।
2. **_blank**: লিঙ্কটি নতুন উইন্ডো বা ট্যাবে খোলে।
3. **_parent**: লিঙ্কটি প্যারেন্ট ফ্রেমে খোলে (যদি কোনও ফ্রেম ব্যবহৃত হয়)।
4. **_top**: লিঙ্কটি পুরো টপ ফ্রেমে খোলে।

`_blank` ব্যবহার করার ফলে, যখন ব্যবহারকারী লিঙ্কে ক্লিক করে, তখন এটি একটি নতুন ব্রাউজার উইন্ডো বা ট্যাব খুলবে, যা মূল পৃষ্ঠাটি ছাড়াই দেখা যাবে। এটি ব্যবহারকারীর জন্য একটি সুবিধাজনক উপায়, কারণ এটি তাদের মূল পৃষ্ঠায় ফিরে আসার জন্য সহজ করে তোলে।

উদাহরণ:
<a href="https://example.com" target="_blank">Visit Example.com in a new tab</a>

এই উদাহরণে, ব্যবহারকারী "Visit Example.com in a new tab" লিঙ্কে ক্লিক করলে একটি নতুন ট্যাবে https://example.com খুলবে।

-->

<!--------------------- Answer Number 10 End --------------------->

<!-- step 2 end -->



<!--------------------- Answer Number 11 Start --------------------->

<!--
ফ্যাভিকন হলো একটি ছোট আইকন যা একটি ওয়েবসাইটের ব্রাউজার ট্যাবে, বুকমার্কে এবং অন্যান্য স্থানে প্রদর্শিত হয়। এটি একটি ওয়েবসাইটের ব্র্যান্ডিংয়ের একটি গুরুত্বপূর্ণ অংশ এবং ব্যবহারকারীদের একটি সহজ চিনতে সহায়তা করে।

ফ্যাভিকন যোগ করার জন্য HTML ডকুমেন্টের `<head>` অংশে একটি `<link>` ট্যাগ ব্যবহার করা হয়। সাধারণত ফ্যাভিকন PNG, ICO, বা GIF ফরম্যাটে থাকে। 

ফ্যাভিকন যুক্ত করার উদাহরণ:

<head>
    <title>My Website</title>
    <link rel="icon" href="favicon.ico" type="image/x-icon">
</head>

এই উদাহরণে, `favicon.ico` নামের একটি ফ্যাভিকন ফাইল `<head>` এর মধ্যে `<link>` ট্যাগ ব্যবহার করে যুক্ত করা হয়েছে। `rel="icon"` অ্যাট্রিবিউটটি নির্দেশ করে যে এটি একটি আইকন, এবং `type="image/x-icon"` নির্দেশ করে যে ফাইলের টাইপ আইকন ফাইল।

ফ্যাভিকন কেন যুক্ত করবেন:
1. **ব্র্যান্ডিং**: এটি একটি ওয়েবসাইটের পরিচয় বাড়ায় এবং ব্যবহারকারীদের জন্য চিনতে সহায়ক।
2. **পেশাদারিত্ব**: একটি ফ্যাভিকন একটি সাইটকে আরও পেশাদার দেখায়।
3. **নেভিগেশন**: ব্যবহারকারীরা সহজেই তাদের বুকমার্কে এবং ট্যাবগুলিতে সাইট চিহ্নিত করতে পারে, যা নেভিগেশন সহজ করে।

-->

<!--------------------- Answer Number 11 End --------------------->


<!--------------------- Answer Number 12 Start --------------------->

<!--
ফরম্যাটিং (Formatting) হল HTML ডকুমেন্টে টেক্সট বা কন্টেন্টের উপস্থাপন এবং দৃশ্যমানতা পরিবর্তন করার প্রক্রিয়া। এটি ওয়েব পেজের পাঠ্যকে আরও পড়তে উপযোগী এবং আকর্ষণীয় করে তোলে। ফরম্যাটিং ট্যাগগুলো মূলত টেক্সটের আকার, রঙ, স্টাইল এবং বিন্যাস নিয়ন্ত্রণ করতে ব্যবহৃত হয়।

HTML এ ফরম্যাটিংয়ের কিছু সাধারণ ট্যাগ হল:

1. `<b>`: এটি টেক্সটকে গা bold করে।
   উদাহরণ: <b>এটি গা bold টেক্সট</b>

2. `<i>`: এটি টেক্সটকে italics করে।
   উদাহরণ: <i>এটি italics টেক্সট</i>

3. `<u>`: এটি টেক্সটের নিচে আন্ডারলাইন দেয়।
   উদাহরণ: <u>এটি আন্ডারলাইন করা টেক্সট</u>

4. `<strong>`: এটি গুরুত্বপূর্ণ টেক্সটকে গা bold করে এবং সাধারণত এটি স্ক্রীন রিডার দ্বারা উচ্চারিত হয়।
   উদাহরণ: <strong>এটি গুরুত্বপূর্ণ টেক্সট</strong>

5. `<em>`: এটি টেক্সটকে italics করে এবং সাধারণত এটি বিশেষ গুরুত্ব দেয়।
   উদাহরণ: <em>এটি বিশেষ গুরুত্ব দেয়া টেক্সট</em>

ফরম্যাটিং ট্যাগ ব্যবহার করার মাধ্যমে, ব্যবহারকারীরা তাদের কন্টেন্টকে আরও আকর্ষণীয় এবং পাঠযোগ্য করে তুলতে পারে, যা একটি ভাল ইউজার এক্সপেরিয়েন্স প্রদান করে।

-->

<!--------------------- Answer Number 12 End --------------------->


<!--------------------- Answer Number 13 Start --------------------->

<!--
HTML5-এ এলিমেন্টগুলি সাধারণত দুটি প্রকারে ভাগ করা হয়: Inline elements এবং Block elements।

1. **Block Elements**:
   - Block elements সাধারণত পূর্ণ প্রস্থ দখল করে এবং একটি নতুন লাইনে শুরু হয়। 
   - এগুলি সাধারণত একটি নতুন সেকশন তৈরি করে এবং এর পরে থাকা এলিমেন্টগুলি নতুন লাইনে থাকে। 
   - উদাহরণ: `<div>`, `<h1>`, `<p>`, `<ul>`, `<li>`, `<section>`, `<article>`।

   **উদাহরণ**:
   ```html
   <div>This is a block element.</div>
   <p>This is another block element.</p>
Inline Elements:

Inline elements সাধারণত তাদের কনটেন্টের প্রস্থ দখল করে এবং একই লাইনে থাকে।
এগুলি একটি ব্লক এলিমেন্টের ভিতরে থাকতে পারে এবং পরবর্তী এলিমেন্টের পরে নতুন লাইন শুরু করে না।
উদাহরণ: <span>, <a>, <img>, <strong>, <em>।
উদাহরণ:

html
Copy code
<p>This is a <strong>strong</strong> inline element inside a paragraph.</p>
<a href="#">This is an inline link.</a>
সারসংক্ষেপ:
Block elements: নতুন লাইনে শুরু হয় এবং সম্পূর্ণ প্রস্থ দখল করে।
Inline elements: একই লাইনে থাকে এবং শুধুমাত্র তাদের কনটেন্টের প্রস্থ দখল করে।
এইভাবে HTML5-এ Inline এবং Block elements-এর পার্থক্য এবং উদাহরণগুলো ব্যাখ্যা করা হলো।

-->

<!--------------------- Answer Number 13 End --------------------->


<!--------------------- Answer Number 14 Start --------------------->

<!--
**Marquee** হল একটি HTML ট্যাগ যা পৃষ্ঠার একটি অংশে টেক্সট বা অন্যান্য কনটেন্টকে স্লাইড বা চলমানভাবে প্রদর্শনের জন্য ব্যবহৃত হয়। এটি সাধারণত ব্যবহারকারীদের দৃষ্টি আকর্ষণ করার জন্য ডিজাইন করা হয়, কিন্তু এই ট্যাগটি HTML5-এ আর সমর্থিত নয় এবং এটি ব্যবহারে অতি ব্যবহৃত হয় না। সুতরাং, এটি ব্যবহার করার আগে অন্যান্য পদ্ধতি বিবেচনা করা উচিত।

### উদাহরণ:
```html
<marquee behavior="scroll" direction="left">
    This is a scrolling marquee text!
</marquee>-->
<!--------------------- Answer Number 14 End --------------------->


<!--------------------- Answer Number 15 Start --------------------->

<!--
**<p> ট্যাগ**:
- **সংজ্ঞা**: এটি একটি প্যারাগ্রাফ ট্যাগ যা মূলত ব্লক স্তরের উপাদান হিসেবে কাজ করে।
- **ব্যবহার**: সাধারণ টেক্সট প্যারাগ্রাফ তৈরি করতে ব্যবহার করা হয়। এটি পৃষ্ঠায় একটি নতুন লাইন শুরু করে।
- **উদাহরণ**:
```html
<p>This is a paragraph of text that describes something.</p>
<span> ট্যাগ:

সংজ্ঞা: এটি একটি ইনলাইন ট্যাগ যা সাধারণত ছোট টেক্সট অংশকে স্টাইল বা হাইলাইট করার জন্য ব্যবহৃত হয়।
ব্যবহার: টেক্সটের একটি অংশে স্টাইলিং যোগ করতে, যেমন রং, ফন্ট সাইজ, বা ব্যাকগ্রাউন্ড পরিবর্তন করতে ব্যবহৃত হয়। এটি কোনো ব্লক তৈরি করে না।
উদাহরণ:
html
Copy code
<span style="color: blue;">This text is blue.</span>
<div> ট্যাগ:

সংজ্ঞা: এটি একটি ব্লক স্তরের উপাদান যা বিভিন্ন উপাদানকে একসাথে গ্রুপ করার জন্য ব্যবহৃত হয়।
ব্যবহার: পৃষ্ঠার বিভিন্ন সেকশন বা এলিমেন্টগুলোর জন্য একটি কন্টেইনার তৈরি করতে ব্যবহৃত হয়, এবং সাধারণত CSS দিয়ে স্টাইল করতে ব্যবহৃত হয়।
উদাহরণ:
html
Copy code
<div style="background-color: lightgray;">
    <h2>This is a heading inside a div</h2>
    <p>This is a paragraph inside the div.</p>
</div>
সারসংক্ষেপ:
<p> ট্যাগ সাধারণ প্যারাগ্রাফ তৈরি করতে ব্যবহৃত হয়।
<span> ট্যাগ ইনলাইন উপাদান হিসেবে ব্যবহৃত হয়, সাধারণত টেক্সটের ছোট অংশের জন্য।
<div> ট্যাগ ব্লক স্তরের উপাদান হিসেবে ব্যবহৃত হয়, বিভিন্ন উপাদানকে গ্রুপ করার জন্য। -->
<!--------------------- Answer Number 15 End --------------------->


<!--------------------- Answer Number 16 Start --------------------->

<!--
**<ul> ট্যাগ**:
- **সংজ্ঞা**: এটি একটি অর্ডারলেস (অর্থাৎ, কোন নির্দিষ্ট ক্রম নেই) তালিকা তৈরি করতে ব্যবহৃত হয়।
- **ব্যবহার**: এটি বিন্দুবদ্ধ তালিকা তৈরি করতে ব্যবহৃত হয়, যেখানে প্রতিটি আইটেম একটি বুলেট পয়েন্টের মাধ্যমে উপস্থাপিত হয়।
- **উদাহরণ**:
```html
<ul>
    <li>First item</li>
    <li>Second item</li>
    <li>Third item</li>
</ul>
<ol> ট্যাগ:

সংজ্ঞা: এটি একটি অর্ডারড (অর্থাৎ, নির্দিষ্ট ক্রম আছে) তালিকা তৈরি করতে ব্যবহৃত হয়।
ব্যবহার: এটি সংখ্যাযুক্ত তালিকা তৈরি করতে ব্যবহৃত হয়, যেখানে প্রতিটি আইটেম একটি সংখ্যা বা অক্ষরের মাধ্যমে উপস্থাপিত হয়।
উদাহরণ:
html
Copy code
<ol>
    <li>First item</li>
    <li>Second item</li>
    <li>Third item</li>
</ol>
<li> ট্যাগ:

সংজ্ঞা: এটি একটি তালিকা আইটেম নির্দেশ করে এবং এটি <ul> বা <ol> ট্যাগের ভিতরে ব্যবহার করা হয়।
ব্যবহার: এটি তালিকার প্রতিটি উপাদানের জন্য ব্যবহৃত হয়।
উদাহরণ:
html
Copy code
<li>This is an item in the list</li>
সারসংক্ষেপ:
<ul> ট্যাগ বিন্দুবদ্ধ তালিকা তৈরি করে।
<ol> ট্যাগ সংখ্যাযুক্ত তালিকা তৈরি করে।
<li> ট্যাগ তালিকার প্রতিটি আইটেম নির্দেশ করে এবং এটি উভয় <ul> এবং <ol> এর ভিতরে ব্যবহৃত হয়। -->
<!--------------------- Answer Number 16 End --------------------->


<!--------------------- Answer Number 17 Start --------------------->

<!--
**<input> ট্যাগ**:
- **সংজ্ঞা**: এটি HTML ফর্মে ব্যবহারকারীর থেকে ইনপুট নেওয়ার জন্য ব্যবহৃত হয়। এটি বিভিন্ন ধরনের ইনপুট ফিল্ড তৈরি করতে পারে, যেমন টেক্সট, রেডিও বাটন, চেকবক্স ইত্যাদি।

### কিছু প্রধান ইনপুট টাইপ এবং তাদের ব্যাখ্যা:

1. **text**:
   - **ব্যবহার**: সাধারণ টেক্সট ইনপুটের জন্য ব্যবহৃত হয়।
   - **উদাহরণ**:
   ```html
   <input type="text" name="username" placeholder="Enter your username">
radio:

ব্যবহার: এটি একাধিক বিকল্পের মধ্যে একটি নির্বাচন করতে ব্যবহৃত হয়। একই নামের রেডিও বাটন গ্রুপের মধ্যে কেবল একটি নির্বাচন করা যায়।
উদাহরণ:
html
Copy code
<input type="radio" name="gender" value="male"> Male
<input type="radio" name="gender" value="female"> Female
checkbox:

ব্যবহার: এটি একাধিক বিকল্পের মধ্যে এক বা একাধিক নির্বাচন করতে ব্যবহৃত হয়। ব্যবহারকারী একাধিক চেকবক্স চেক করতে পারে।
উদাহরণ:
html
Copy code
<input type="checkbox" name="subscribe" value="newsletter"> Subscribe to newsletter
সারসংক্ষেপ:
<input> ট্যাগ ফর্মে বিভিন্ন ধরনের ইনপুট সংগ্রহ করার জন্য ব্যবহৃত হয়।
type অ্যাট্রিবিউটের মাধ্যমে ইনপুটের ধরন নির্ধারণ করা হয়, যেমন text, radio, এবং checkbox।
প্রতিটি ইনপুট টাইপের বিভিন্ন বৈশিষ্ট্য এবং ব্যবহার রয়েছে, যা ব্যবহারকারীর ইন্টারঅ্যাকশনের জন্য উপকারী।
-->

<!--------------------- Answer Number 17 End --------------------->


<!--------------------- Answer Number 18 Start --------------------->

<!--
**<button>, <textarea>, এবং <select> ট্যাগ**:
- এই ট্যাগগুলো HTML ফর্মে বিভিন্ন ধরনের ইনপুট এবং ইউজার ইন্টারঅ্যাকশন তৈরি করতে ব্যবহৃত হয়।

### 1. <button>:
- **ব্যবহার**: এটি একটি বাটন তৈরি করতে ব্যবহৃত হয়, যা ক্লিক করা যেতে পারে। সাধারণত এটি ফর্মের ডাটা সাবমিট করার জন্য অথবা JavaScript ইভেন্ট পরিচালনার জন্য ব্যবহৃত হয়।
- **উদাহরণ**:
```html
<button type="submit">Submit</button>
2. <textarea>:
ব্যবহার: এটি একটি মাল্টি-লাইনের টেক্সট ইনপুট ফিল্ড তৈরি করতে ব্যবহৃত হয়। ব্যবহারকারীরা এতে বড় পরিমাণে টেক্সট লিখতে পারে।
উদাহরণ:
html
Copy code
<textarea name="message" rows="4" cols="50" placeholder="Type your message here..."></textarea>
3. <select>:
ব্যবহার: এটি একটি ড্রপডাউন মেনু তৈরি করতে ব্যবহৃত হয়, যেখানে ব্যবহারকারী একাধিক অপশন থেকে একটি নির্বাচন করতে পারে। এটি <option> ট্যাগের সাথে ব্যবহৃত হয়।
উদাহরণ:
html
Copy code
<select name="fruits">
    <option value="apple">Apple</option>
    <option value="banana">Banana</option>
    <option value="orange">Orange</option>
</select>
সারসংক্ষেপ:
<button> ট্যাগ ব্যবহারকারীকে কোনো কার্যক্রম শুরু করার জন্য ক্লিকযোগ্য বাটন প্রদান করে।
<textarea> ট্যাগ ব্যবহারকারীকে একটি মাল্টি-লাইনের টেক্সট ইনপুট করার সুযোগ দেয়।
<select> ট্যাগ ব্যবহারকারীকে একটি ড্রপডাউন মেনুর মাধ্যমে একটি অপশন নির্বাচন করার সুযোগ দেয়।
-->

<!--------------------- Answer Number 18 End --------------------->

<!--------------------- Answer Number 19 Start --------------------->

<!--
**CSS কে HTML এর সাথে লিংক করার পদ্ধতি**:
HTML ডকুমেন্টে CSS যুক্ত করার জন্য তিনটি পদ্ধতি রয়েছে: Inline CSS, Internal CSS, এবং External CSS।

### 1. Inline CSS:
- **ব্যবহার**: HTML এলিমেন্টের সাথে সরাসরি `style` অ্যাট্রিবিউট ব্যবহার করে CSS স্টাইল প্রয়োগ করা হয়।
- **উদাহরণ**:
```html
<p style="color: blue; font-size: 20px;">This is a blue paragraph.</p>
2. Internal CSS:
ব্যবহার: <style> ট্যাগ ব্যবহার করে HTML ডকুমেন্টের <head> সেকশনে CSS লেখা হয়।
উদাহরণ:
html
Copy code
<head>
    <style>
        body {
            background-color: lightgrey;
        }
        h1 {
            color: green;
        }
    </style>
</head>
3. External CSS:
ব্যবহার: আলাদা CSS ফাইল তৈরি করে সেই ফাইলকে HTML ডকুমেন্টের <head> সেকশনে <link> ট্যাগ ব্যবহার করে যুক্ত করা হয়। এই পদ্ধতি সবচেয়ে বেশি ব্যবহৃত হয় কারণ এটি কোডের পুনরাবৃত্তি কমায় এবং রক্ষণাবেক্ষণ সহজ করে।
উদাহরণ:
html
Copy code
<head>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
সারসংক্ষেপ:
CSS কে HTML এর সাথে লিংক করার জন্য Inline, Internal, এবং External পদ্ধতি ব্যবহার করা হয়। External CSS ব্যবহার করলে কোডের পুনরাবৃত্তি কমে এবং রক্ষণাবেক্ষণ করা সহজ হয়।
-->

<!--------------------- Answer Number 19 End --------------------->


<!--------------------- Answer Number 20 Start --------------------->

<!--
**<label> ট্যাগ**:
- **ব্যাখ্যা**: `<label>` ট্যাগ HTML ফর্মের ইনপুট এলিমেন্টগুলোর জন্য একটি ট্যাগ যা ব্যবহারকারীদের বোঝাতে সহায়তা করে যে কোন ইনপুটের জন্য কোন লেবেল রয়েছে। এটি একটি পাঠ্য ট্যাগ হিসেবে কাজ করে এবং এটি ব্যবহারকারীর জন্য ফর্ম ইনপুটের উদ্দেশ্য বুঝতে সহায়ক।

- **সহযোগিতা**: `<label>` ট্যাগের সাথে একটি ইনপুট এলিমেন্ট যুক্ত করার জন্য, `<label>` ট্যাগের `for` অ্যাট্রিবিউট ব্যবহার করা হয়, যা ইনপুট এলিমেন্টের `id` এর সাথে মিলে যায়। যখন ব্যবহারকারী লেবেলে ক্লিক করে, তখন সংশ্লিষ্ট ইনপুট ফোকাস হয়ে যায়।

**উদাহরণ**:
```html
<form>
    <label for="username">Username:</label>
    <input type="text" id="username" name="username">
    
    <label for="password">Password:</label>
    <input type="password" id="password" name="password">
    
    <input type="submit" value="Submit">
</form>
সারসংক্ষেপ:
<label> ট্যাগ ইনপুট এলিমেন্টের জন্য একটি বর্ণনামূলক লেবেল প্রদান করে এবং এটি ব্যবহারকারীকে ইনপুট ফিল্ডটি চিহ্নিত করতে সাহায্য করে। এটি for অ্যাট্রিবিউটের মাধ্যমে ইনপুটের সাথে যুক্ত হয়, যা ব্যবহারকারীর জন্য ব্যবহারযোগ্যতা উন্নত করে।
-->

<!--------------------- Answer Number 20 End --------------------->

<!--------------------- Answer Number 21 Start --------------------->

<!--
**<iframe> ট্যাগ**:
- **ব্যাখ্যা**: `<iframe>` ট্যাগ HTML ডকুমেন্টের মধ্যে একটি ইনলাইন ফ্রেম তৈরি করে যা অন্য একটি HTML পেজ বা কন্টেন্ট প্রদর্শন করার জন্য ব্যবহৃত হয়। এটি পেজের মধ্যে বিভিন্ন ধরণের কন্টেন্ট যেমন একটি অন্য ওয়েব পৃষ্ঠা, ভিডিও, বা মানচিত্র ইত্যাদি এম্বেড করার জন্য উপকারী।

- **গুরুত্বপূর্ণ অ্যাট্রিবিউটসমূহ**:
  1. **src**: এটি ইনলাইন ফ্রেমে প্রদর্শিত কন্টেন্টের URL নির্দেশ করে।
  2. **width**: ইনলাইন ফ্রেমের প্রস্থ নির্ধারণ করে।
  3. **height**: ইনলাইন ফ্রেমের উচ্চতা নির্ধারণ করে।
  4. **frameborder**: এটি ফ্রেমের চারপাশে একটি বর্ডার তৈরি করে (প্রায়শই 0 বা 1 সেট করা হয়)।
  5. **allowfullscreen**: এই অ্যাট্রিবিউটটি ফ্রেমে পূর্ণ পর্দার মোড সক্রিয় করতে ব্যবহৃত হয়।

**উদাহরণ**:
```html
<iframe src="https://www.example.com" width="600" height="400" frameborder="0" allowfullscreen>
    Your browser does not support iframes.
</iframe>
সারসংক্ষেপ:
<iframe> ট্যাগ ব্যবহৃত হয় অন্য একটি HTML পেজ বা কন্টেন্ট প্রদর্শনের জন্য। এর সাথে সাধারণত src, width, height, frameborder, এবং allowfullscreen অ্যাট্রিবিউট ব্যবহৃত হয়।
-->

<!--------------------- Answer Number 21 End --------------------->


<!--------------------- Answer Number 22 Start --------------------->

<!--
**<audio> এবং <video> ট্যাগ**:
- **ব্যাখ্যা**: `<audio>` এবং `<video>` ট্যাগ HTML5 তে ব্যবহার করা হয় অডিও এবং ভিডিও ফাইল এম্বেড করার জন্য। এটি ব্যবহারকারীদের ওয়েব পৃষ্ঠায় অডিও বা ভিডিও কন্টেন্ট শোনার বা দেখার সুবিধা দেয়।

### <audio> ট্যাগ:
- **উদ্দেশ্য**: এটি একটি অডিও ফাইল এম্বেড করতে ব্যবহৃত হয়।
- **গুরুত্বপূর্ণ অ্যাট্রিবিউটসমূহ**:
  1. **controls**: প্লেব্যাক কন্ট্রোল যেমন প্লে, পজ, এবং ভলিউম কন্ট্রোল প্রদর্শন করে।
  2. **autoplay**: পৃষ্ঠা লোড হওয়ার সাথে সাথে অডিও স্বয়ংক্রিয়ভাবে চালু হয়।
  3. **loop**: অডিও ফাইলটি শেষ হলে এটি পুনরায় শুরু হয়।

**উদাহরণ**:
```html
<audio controls autoplay loop>
    <source src="audio-file.mp3" type="audio/mpeg">
    Your browser does not support the audio tag.
</audio>
<video> ট্যাগ:
উদ্দেশ্য: এটি একটি ভিডিও ফাইল এম্বেড করতে ব্যবহৃত হয়।
গুরুত্বপূর্ণ অ্যাট্রিবিউটসমূহ:
controls: ভিডিও প্লেব্যাক কন্ট্রোল প্রদর্শন করে।
autoplay: ভিডিও পৃষ্ঠা লোড হওয়ার সাথে সাথে স্বয়ংক্রিয়ভাবে চালু হয়।
loop: ভিডিওটি শেষ হলে এটি পুনরায় শুরু হয়।
width এবং height: ভিডিওর প্রস্থ এবং উচ্চতা নির্ধারণ করে।
উদাহরণ:

html
Copy code
<video controls width="600" height="400" autoplay loop>
    <source src="video-file.mp4" type="video/mp4">
    Your browser does not support the video tag.
</video>
সারসংক্ষেপ:
<audio> এবং <video> ট্যাগ অডিও এবং ভিডিও কন্টেন্ট এম্বেড করার জন্য ব্যবহৃত হয়। তাদের সাথে সাধারণত controls, autoplay, এবং loop অ্যাট্রিবিউট ব্যবহৃত হয়।
-->

<!--------------------- Answer Number 22 End --------------------->

<!--------------------- Answer Number 23 Start --------------------->

<!--
**<picture> ট্যাগ**:
- **ব্যাখ্যা**: `<picture>` ট্যাগ HTML5 তে একটি নতুন বৈশিষ্ট্য যা ছবি লোড করার জন্য ব্যবহার করা হয়। এটি বিভিন্ন স্ক্রীন আকার এবং ডিভাইস অনুযায়ী ভিন্ন ছবি প্রদর্শনের অনুমতি দেয়। এটি বিশেষত রেসপন্সিভ ডিজাইনে উপকারী, যেখানে বিভিন্ন রেজোলিউশনের ছবি প্রদর্শনের প্রয়োজন হয়।

### ব্যবহার:
- `<picture>` ট্যাগের মধ্যে `<source>` ট্যাগ ব্যবহার করা হয়, যা ব্রাউজারের স্ক্রীন সাইজ এবং রেজোলিউশনের উপর ভিত্তি করে সঠিক ছবি নির্বাচন করে। এর পরে একটি `<img>` ট্যাগ ব্যবহার করা হয় যাতে ছবিটি প্রদর্শন করা হয় যদি কোন `<source>` ট্যাগের শর্ত পূরণ না হয়।

### উদাহরণ:
```html
<picture>
    <source media="(max-width: 600px)" srcset="small-image.jpg">
    <source media="(max-width: 1200px)" srcset="medium-image.jpg">
    <img src="large-image.jpg" alt="A description of the image">
</picture>
উদাহরণ ব্যাখ্যা:
<source>:
প্রথম <source> ট্যাগটি একটি শর্ত নির্ধারণ করে যা বলছে, যদি স্ক্রীনের প্রস্থ 600 পিক্সেলের কম হয়, তাহলে small-image.jpg লোড করা হবে।
দ্বিতীয় <source> ট্যাগটি বলছে, যদি স্ক্রীনের প্রস্থ 1200 পিক্সেলের কম হয়, তাহলে medium-image.jpg লোড করা হবে।
<img>:
যদি কোন শর্ত পূরণ না হয় (যেমন যদি স্ক্রীনের প্রস্থ 1200 পিক্সেলের বেশি হয়), তাহলে ডিফল্টভাবে large-image.jpg লোড হবে।
সারসংক্ষেপ:
<picture> ট্যাগ রেসপন্সিভ ডিজাইনে বিভিন্ন ছবি প্রদর্শনের জন্য একটি শক্তিশালী টুল, যা বিভিন্ন ডিভাইসে সঠিক ছবি লোড করতে সাহায্য করে।
-->

<!--------------------- Answer Number 23 End --------------------->


<!--------------------- Answer Number 24 Start --------------------->

<!--
**ফর্ম ডিজাইন**:
- এই ফর্মে একটি নাম ইনপুট ক্ষেত্র, একটি লিঙ্গের জন্য রেডিও বোতাম, একটি চেকবক্স এবং একটি সাবমিট বোতাম রয়েছে।

### উদাহরণ:
```html
<form action="/submit" method="post">
    <label for="name">নাম:</label>
    <input type="text" id="name" name="name" required>

    <fieldset>
        <legend>লিঙ্গ:</legend>
        <label>
            <input type="radio" name="gender" value="male" required> পুরুষ
        </label>
        <label>
            <input type="radio" name="gender" value="female"> মহিলা
        </label>
    </fieldset>

    <label>
        <input type="checkbox" name="subscribe" value="yes"> নিউজলেটারে সাবস্ক্রাইব করুন
    </label>

    <button type="submit">সাবমিট করুন</button>
</form>
উদাহরণ ব্যাখ্যা:
<form>: এই ট্যাগটি ফর্মের শুরু এবং এটি action অ্যাট্রিবিউটের মাধ্যমে নির্দিষ্ট করে যে ডেটা কোথায় পাঠানো হবে এবং method দ্বারা HTTP পদ্ধতি (যেমন post) নির্ধারণ করে।

<label>: প্রতিটি ইনপুটের জন্য একটি লেবেল ট্যাগ রয়েছে, যা ব্যবহারকারীর জন্য ইনপুট ক্ষেত্রকে চিহ্নিত করে।

<input type="text">: একটি টেক্সট ইনপুট ক্ষেত্র, যেখানে ব্যবহারকারী তার নাম লিখতে পারে।

<fieldset> & <legend>: এই ট্যাগগুলি একসাথে একটি গ্রুপের জন্য ব্যবহার করা হয়, এখানে লিঙ্গ নির্বাচন করার জন্য ব্যবহৃত হচ্ছে।

<input type="radio">: পুরুষ এবং মহিলার জন্য দুটি রেডিও বোতাম।

<input type="checkbox">: একটি চেকবক্স যা নিউজলেটারে সাবস্ক্রাইব করার অপশন দেয়।

<button type="submit">: ফর্মটি জমা দেওয়ার জন্য একটি বোতাম।

সারসংক্ষেপ:
এই ফর্মটি মৌলিক ইনপুটগুলোর উদাহরণ দেয় এবং ব্যবহারকারীর থেকে তথ্য সংগ্রহের জন্য ডিজাইন করা হয়েছে।
-->

<!--------------------- Answer Number 24 End --------------------->



<!--------------------- Answer Number 25 Start --------------------->

<!--
**ইউটিউব ভিডিও এমবেড**:
- এখানে একটি ইউটিউব ভিডিও `<iframe>` ট্যাগের মাধ্যমে এমবেড করা হয়েছে, যা প্রতিক্রিয়া যোগ্যভাবে ডিজাইন করা হয়েছে।

### উদাহরণ:
```html
<div style="position: relative; padding-bottom: 56.25%; height: 0;">
    <iframe 
        src="https://www.youtube.com/embed/dQw4w9WgXcQ" 
        style="position: absolute; top: 0; left: 0; width: 100%; height: 100%;"
        frameborder="0" 
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
        allowfullscreen>
    </iframe>
</div>
উদাহরণ ব্যাখ্যা:
<div>: একটি ডিভ যা ভিডিওর জন্য একটি কনটেইনার হিসাবে কাজ করে। এটি padding-bottom ব্যবহার করে 16:9 অ্যাস্পেক্ট রেশিও নিশ্চিত করে।

<iframe>:

src: ইউটিউব ভিডিওর লিংক প্রদান করে। এখানে উদাহরণস্বরূপ একটি ভিডিও লিংক দেওয়া হয়েছে।
style: এটি ভিডিওকে পূর্ণ প্রস্থ এবং উচ্চতা নিশ্চিত করে, যাতে এটি কনটেইনারে সঠিকভাবে ফিট করে।
frameborder: এটি ফ্রেমের চারপাশে একটি সীমা তৈরি করতে ব্যবহৃত হয়। এখানে এটি 0 সেট করা হয়েছে।
allow: এই অ্যাট্রিবিউটটি বিভিন্ন সুবিধা যেমন অটোপ্লে, গাইরোস্কোপ এবং পিকচার-ইন-পিকচার মোড অনুমোদন করে।
allowfullscreen: এটি ভিডিওকে ফুলস্ক্রীনে খোলার অনুমতি দেয়।
সারসংক্ষেপ:
এই <iframe> ব্যবহার করে একটি ইউটিউব ভিডিও এমবেড করা হয়েছে যা প্রতিক্রিয়া যোগ্য। ব্যবহারকারী স্ক্রীন আকারের উপর ভিত্তি করে ভিডিওটি দেখতে পারে।
-->

<!--------------------- Answer Number 25 End --------------------->


<!--------------------- Answer Number 26 Start --------------------->

<!--
**সংকোচনযোগ্য/বিস্তৃত অংশ**:
- এখানে `<details>` এবং `<summary>` ট্যাগ ব্যবহার করে একটি সংকোচনযোগ্য বিভাগ তৈরি করা হয়েছে।

### উদাহরণ:
```html
<details>
    <summary>এখানে ক্লিক করুন</summary>
    <p>এই অংশটি সংকোচিত আছে। আপনি যদি বিস্তারিত জানার জন্য ক্লিক করেন, তাহলে এই তথ্যটি প্রদর্শিত হবে।</p>
    <p>এটি একটি উদাহরণ টেক্সট যা আপনি সংকোচিত বিভাগে দেখতে পাবেন।</p>
</details>
উদাহরণ ব্যাখ্যা:
<details>: এটি একটি সংকোচনযোগ্য এলিমেন্ট যা ব্যবহারকারীর ক্লিক করার জন্য বিভিন্ন তথ্য ধারণ করে। এটি ডিফল্টভাবে বন্ধ থাকে।

<summary>: এটি একটি সারসংক্ষেপ এলিমেন্ট যা ব্যবহারকারী যখন ক্লিক করবে তখন এটি সংকোচিত অংশটি দেখাবে। এটি সংক্ষেপে প্রদর্শিত হয় এবং ব্যবহারকারীর ক্লিক করার জন্য একটি ইন্টারেক্টিভ এলিমেন্ট।

<p>: এটি প্যারাগ্রাফ ট্যাগ, যা সংকোচিত অংশের মধ্যে তথ্য প্রদর্শন করতে ব্যবহৃত হয়।

সারসংক্ষেপ:
এই উদাহরণটি একটি সহজ সংকোচনযোগ্য/বিস্তৃত বিভাগ তৈরি করে, যা ব্যবহারকারী ক্লিক করার মাধ্যমে খুলতে এবং বন্ধ করতে পারে।
-->

<!--------------------- Answer Number 26 End --------------------->



<!--------------------- Answer Number 27 Start --------------------->

<!--
**নেভিগেশন বার**:
- এখানে `<nav>`, `<ul>`, এবং `<li>` ট্যাগ ব্যবহার করে একটি সাধারণ নেভিগেশন বার তৈরি করা হয়েছে।

### উদাহরণ:
```html
<nav>
    <ul>
        <li><a href="#home">হোম</a></li>
        <li><a href="#about">আমাদের সম্পর্কে</a></li>
        <li><a href="#contact">যোগাযোগ</a></li>
    </ul>
</nav>
উদাহরণ ব্যাখ্যা:
<nav>: এটি একটি নেভিগেশন এলিমেন্ট, যা সাইটের নেভিগেশন লিঙ্কগুলি ধারণ করে।

<ul>: এটি একটি অর্ডারবিহীন তালিকা, যা নেভিগেশন লিঙ্কগুলির তালিকা তৈরি করতে ব্যবহৃত হয়।

<li>: এটি তালিকার একটি আইটেম, যেখানে প্রতিটি লিঙ্ক প্রতিনিধিত্ব করে।

<a>: এটি একটি লিঙ্ক ট্যাগ, যেখানে href অ্যাট্রিবিউট ব্যবহার করে নির্দিষ্ট URL বা স্থানচিহ্নের দিকে নির্দেশ করে।

সারসংক্ষেপ:
এই উদাহরণটি একটি সহজ নেভিগেশন বার তৈরি করে, যা ব্যবহারকারীকে বিভিন্ন পৃষ্ঠায় নিয়ে যেতে সাহায্য করে।
-->

<!--------------------- Answer Number 27 End --------------------->


<!--------------------- Answer Number 28 Start --------------------->

<!--
**জটিল টেবিল**:
- এখানে একটি টেবিল তৈরি করা হয়েছে যেখানে সেলগুলি মিশ্রিত করা হয়েছে, এবং টেবিলের বিভিন্ন অংশ চিহ্নিত করা হয়েছে।

### উদাহরণ:
```html
<table border="1">
    <caption>মাসিক বিক্রয় প্রতিবেদন</caption>
    <thead>
        <tr>
            <th rowspan="2">পণ্য</th>
            <th colspan="3">বিক্রয় সংখ্যা</th>
        </tr>
        <tr>
            <th>জানুয়ারী</th>
            <th>ফেব্রুয়ারী</th>
            <th>মার্চ</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <th>পণ্য A</th>
            <td>150</td>
            <td>200</td>
            <td>250</td>
        </tr>
        <tr>
            <th>পণ্য B</th>
            <td>100</td>
            <td>150</td>
            <td>300</td>
        </tr>
        <tr>
            <th>পণ্য C</th>
            <td>200</td>
            <td>250</td>
            <td>350</td>
        </tr>
    </tbody>
    <tfoot>
        <tr>
            <th>মোট</th>
            <td>450</td>
            <td>600</td>
            <td>900</td>
        </tr>
    </tfoot>
</table>
উদাহরণ ব্যাখ্যা:
<caption>: টেবিলের শিরোনাম প্রদান করে, যা টেবিলের বিষয়বস্তু সম্পর্কে ধারণা দেয়।

<thead>: টেবিলের হেডার অংশ, যা কলাম শিরোনাম ধারণ করে।

<tbody>: টেবিলের মূল তথ্য অংশ।

<tfoot>: টেবিলের শেষ অংশ, যেখানে মোট বা সারাংশ তথ্য রাখা হয়।

colspan: একটি ট্যাগে কয়েকটি কলামকে একত্রিত করতে ব্যবহৃত হয়।

rowspan: একটি ট্যাগে কয়েকটি সারিকে একত্রিত করতে ব্যবহৃত হয়।

সারসংক্ষেপ:
এই টেবিলটি মাসিক বিক্রয়ের তথ্য প্রদর্শন করে, যেখানে বিভিন্ন পণ্যের বিক্রয় সংখ্যা দেখা যাচ্ছে এবং অ্যাক্সেসিবিলিটির জন্য সঠিকভাবে <th> উপাদান ব্যবহার করা হয়েছে।
-->

<!--------------------- Answer Number 28 End --------------------->


<!--------------------- Answer Number 29 Start --------------------->

<!--
**ওয়েবপেজ লেআউট**:
- এখানে একটি সিম্পল লেআউট তৈরি করা হয়েছে যা হেডার, মেইন সেকশন এবং ফুটার সমন্বিত।

### উদাহরণ:
```html
<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>সাধারণ ওয়েবপেজ লেআউট</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #4CAF50;
            color: white;
            padding: 10px 0;
            text-align: center;
        }
        main {
            padding: 20px;
            background-color: #f4f4f4;
        }
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px 0;
            position: relative;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>

<header>
    <h1>আমার ওয়েবসাইট</h1>
</header>

<main>
    <h2>স্বাগতম!</h2>
    <p>এটি একটি সাধারণ ওয়েবপেজ লেআউটের উদাহরণ। এখানে আপনি বিভিন্ন বিষয়বস্তু যোগ করতে পারেন।</p>
    <p>এই সেকশনে আপনি আপনার ধারণা, ব্লগ পোস্ট, বা অন্য যেকোনো তথ্য শেয়ার করতে পারেন।</p>
</main>

<footer>
    <p>© 2024 আমার ওয়েবসাইট। সকল অধিকার সংরক্ষিত।</p>
</footer>

</body>
</html>
উদাহরণ ব্যাখ্যা:
<header>: ওয়েবপেজের শিরোনাম বা পরিচয় প্রদর্শন করে। এখানে একটি প্রধান শিরোনাম রয়েছে।

<main>: ওয়েবপেজের মূল বিষয়বস্তু ধারণ করে, যেখানে বিভিন্ন তথ্য রাখা হয়েছে।

<footer>: ওয়েবপেজের নিচের অংশ, যেখানে সাধারণত কপিরাইট বা অন্যান্য তথ্য থাকে।

সারসংক্ষেপ:
এই উদাহরণটি একটি সহজ ওয়েবপেজ লেআউট তৈরি করে যেখানে সেমান্টিক ট্যাগগুলির ব্যবহার করা হয়েছে। এটি ব্যবহারকারীদের জন্য তথ্যগুলি সহজেই বোঝার এবং নেভিগেট করার সুযোগ দেয়।
-->

<!--------------------- Answer Number 29 End --------------------->


<!--------------------- Answer Number 30 Start --------------------->

<!--
### ১. <html>
- এটি HTML ডকুমেন্টের রুট এলিমেন্ট। এটি ওয়েবপেজের সবকিছু ধারণ করে এবং ডকুমেন্টের শুরুতে থাকা দরকার।

### ২. <head>
- এটি মেটাডেটা ধারণ করে, যেমন ডকুমেন্টের শিরোনাম, স্টাইলশীট এবং স্ক্রিপ্ট। এটি ওয়েবপেজের গুরুত্বপূর্ণ তথ্য জমা করে।

### ৩. <title>
- এটি ব্রাউজারের ট্যাবে প্রদর্শিত শিরোনাম নির্ধারণ করে। এটি SEO এর জন্যও গুরুত্বপূর্ণ।

### ৪. <body>
- এটি ওয়েবপেজের দৃশ্যমান অংশ ধারণ করে। সমস্ত কন্টেন্ট যেমন টেক্সট, ছবি, লিঙ্ক ইত্যাদি এখানে থাকে।

### ৫. <h1> থেকে <h6>
- এগুলো বিভিন্ন স্তরের শিরোনাম তৈরি করে। এগুলোর মাধ্যমে কন্টেন্টের গুরুত্ব বোঝানো হয়, যেখানে <h1> সর্বোচ্চ এবং <h6> সর্বনিম্ন।

### ৬. <p>
- এটি প্যারাগ্রাফ তৈরি করে। টেক্সটের ব্যাখ্যা এবং পাঠ্য বিশ্লেষণের জন্য এটি গুরুত্বপূর্ণ।

### ৭. <a>
- এটি লিঙ্ক তৈরি করে, যা ব্যবহারকারীদের এক পেজ থেকে অন্য পেজে নিয়ে যায়। এটি নেভিগেশন এবং তথ্যের মধ্যে সংযোগ স্থাপন করে।

### ৮. <img>
- এটি ছবি প্রদর্শনের জন্য ব্যবহৃত হয়। ওয়েবপেজের ভিজ্যুয়াল আপীল বাড়াতে এবং তথ্য চিত্রিত করতে সাহায্য করে।

### ৯. <div>
- এটি কন্টেন্টকে গ্রুপ করতে ব্যবহৃত হয় এবং CSS এর মাধ্যমে স্টাইলিং করার জন্য খুবই কার্যকর।

### ১০. <form>
- এটি ব্যবহারকারীদের ইনপুট নেওয়ার জন্য ব্যবহৃত হয়। এটি ডেটা সংগ্রহের জন্য গুরুত্বপূর্ণ এবং ইন্টারেকটিভিটি বাড়ায়।

### সারসংক্ষেপ:
এই ট্যাগগুলো আমাকে আমার ওয়েব ডেভেলপমেন্টে সাহায্য করে এবং আমি এগুলোকে আমার প্রকল্পে নিয়মিত ব্যবহার করি। প্রতিটি ট্যাগের নিজস্ব কাজ এবং উদ্দেশ্য আছে, যা আমাকে আমার কাজকে আরও কার্যকর ও সুন্দর করতে সাহায্য করে।

-->

<!--------------------- Answer Number 30 End --------------------->













<!-- ------------------------------------------------css------------------------------ -->
// 1. **CSS কী, এবং এটি HTML এর সাথে কীভাবে কাজ করে?**
/* CSS (Cascading Style Sheets) একটি স্টাইলিং ল্যাঙ্গুয়েজ যা HTML এ স্টাইল যোগ করার জন্য ব্যবহৃত হয়। HTML কন্টেন্টের গঠন তৈরি করে এবং CSS সেই কন্টেন্টের ডিজাইন বা স্টাইলিং নির্ধারণ করে, যেমন রঙ, ফন্ট, মার্জিন, প্যাডিং ইত্যাদি। HTML এবং CSS একসাথে মিলে একটি ওয়েবপেজকে আকর্ষণীয় ও ব্যবহারযোগ্য করে তোলে। */

// 2. **Inline, Internal এবং External CSS এর মধ্যে পার্থক্য কী?**
/* Inline CSS সরাসরি HTML ট্যাগের মধ্যে style অ্যাট্রিবিউট ব্যবহার করে দেওয়া হয়। Internal CSS একটি HTML ডকুমেন্টের `<head>` ট্যাগে স্টাইল কোড যুক্ত করে। External CSS একটি আলাদা `.css` ফাইলে থাকে এবং HTML ডকুমেন্টে `<link>` ট্যাগ দিয়ে যুক্ত করা হয়। External CSS ব্যবহার করলে একটি CSS ফাইল বিভিন্ন HTML পেজে ব্যবহার করা যায়, যা স্টাইলিং কার্যক্রমকে সহজ করে। */

// 3. **CSS Box Model কী? এটি কোন কোন প্রপার্টির সমন্বয়ে গঠিত?**
/* CSS Box Model এমন একটি কাঠামো যা HTML এলিমেন্টের স্টাইলিং নির্ধারণ করতে ব্যবহৃত হয়। এটি চারটি প্রপার্টির উপর ভিত্তি করে তৈরি: Content, Padding, Border, এবং Margin। Content মূল টেক্সট বা কন্টেন্ট ধরে রাখে। Padding কন্টেন্ট এবং বর্ডারের মধ্যকার স্থান নির্দেশ করে। Border কন্টেন্টের চারপাশের সীমা নির্ধারণ করে এবং Margin এলিমেন্টের চারপাশে বাইরের ফাঁকা স্থান যোগ করে। */

// 4. **CSS এ Specificity কী, এবং এটি কীভাবে স্টাইলিং এ প্রভাব ফেলে?**
/* CSS Specificity নির্দেশ করে কোন সিলেক্টরের স্টাইল বেশি প্রাধান্য পাবে। এটি সংখ্যায় নির্ধারিত হয় এবং কিছু সিলেক্টর অন্যদের চেয়ে বেশি প্রাধান্য পায়। যেমন, ID সিলেক্টর ক্লাস সিলেক্টরের চেয়ে বেশি প্রাধান্য পায়। Specificity বেশি থাকলে সেই স্টাইলটি প্রাধান্য পাবে এবং অন্য একই প্রপার্টির স্টাইলকে ওভাররাইড করবে। */

// 5. **CSS Selectors কীভাবে কাজ করে, এবং কিছু সাধারণ Selector এর উদাহরণ কী?**
/* CSS Selectors হল এমন কোড যা HTML এলিমেন্ট সিলেক্ট করতে ব্যবহৃত হয়। এগুলোর মাধ্যমে আমরা নির্দিষ্ট এলিমেন্টে স্টাইল প্রয়োগ করতে পারি। সাধারণ সিলেক্টরের উদাহরণ হলো: Tag Selector (e.g., `p`), Class Selector (e.g., `.className`), ID Selector (e.g., `#idName`), এবং Universal Selector (e.g., `*`)। */

// 6. **Pseudo-classes এবং Pseudo-elements কী? প্রতিটির উদাহরণ দিন।**
/* Pseudo-classes এমন স্টাইলিং প্রপার্টি যা HTML এলিমেন্টের নির্দিষ্ট অবস্থা নির্দেশ করে, যেমন `:hover` বা `:focus`। উদাহরণ: `a:hover { color: red; }` */
/* Pseudo-elements HTML এলিমেন্টের নির্দিষ্ট অংশে স্টাইল প্রয়োগ করে, যেমন `::before` এবং `::after`। উদাহরণ: `p::before { content: "Note: "; }` */

// 7. **CSS এ Inheritance ধারণা কী?**
/* Inheritance হল CSS এর এমন একটি বৈশিষ্ট্য যেখানে কিছু প্রপার্টি প্যারেন্ট এলিমেন্ট থেকে চাইল্ড এলিমেন্টে স্বয়ংক্রিয়ভাবে উত্তরাধিকার সূত্রে পায়। যেমন, যদি একটি ডিভের ফন্ট সাইজ নির্ধারণ করা থাকে, তাহলে তার ভিতরের ট্যাগগুলো সেই ফন্ট সাইজ পাবে। */

// 8. **CSS এ `!important` নিয়ম কী, এবং এটি কখন ব্যবহার করা উচিত?**
/* `!important` নিয়ম ব্যবহার করলে সেই স্টাইলটি সকল Specificity নিয়ম ওভাররাইড করে। এটি ব্যবহারের সময় সতর্কতা প্রয়োজন, কারণ এটি নির্দিষ্ট এলিমেন্টে সর্বোচ্চ প্রাধান্য প্রদান করে এবং অন্যান্য স্টাইলগুলোকে অগ্রাহ্য করতে পারে। শুধুমাত্র অতি প্রয়োজন হলে এটি ব্যবহার করা উচিত। */



/* second section  */


/* 
1. **`display` প্রপার্টি কীভাবে কাজ করে এবং বিভিন্ন `display` ভ্যালু কী কী?**

**উত্তর:** `display` প্রপার্টি HTML এলিমেন্টগুলোর প্রদর্শন কেমন হবে তা নিয়ন্ত্রণ করে। সাধারণত ব্যবহৃত ভ্যালুগুলো হলো:
- `block`: একটি এলিমেন্ট পুরো লাইনে থাকে এবং নতুন লাইনে শুরু হয়। 
- `inline`: এটি একই লাইনে থাকে এবং আশেপাশের কনটেন্টকে প্রভাবিত করে না।
- `flex`: এলিমেন্টগুলোর মধ্যে জায়গা ভাগাভাগি করে ফ্লেক্সবক্স তৈরি করে।
- `grid`: এলিমেন্টগুলোকে গ্রিড আকারে সাজায়।

**উদাহরণ:*/

.container {
  display: flex;
}

/* 
2. **`position` প্রপার্টি কী এবং বিভিন্ন পজিশন ভ্যালু (static, relative, absolute, fixed, sticky) কিভাবে কাজ করে?**

**উত্তর:** `position` প্রপার্টি দিয়ে এলিমেন্টের অবস্থান নির্ধারণ করা হয়:
- `static`: ডিফল্ট ভ্যালু, সাধারণত কনটেন্টের মতোই থাকে।
- `relative`: ডিফল্ট অবস্থান থেকে অফসেট করে।
- `absolute`: সরাসরি প্যারেন্ট এলিমেন্টের উপর নির্ভর করে।
- `fixed`: স্ক্রলিং করলেও স্থির থাকে।
- `sticky`: নির্দিষ্ট পজিশনে স্থির থাকে যখন স্ক্রলিং করে।

**উদাহরণ:*/

.header {
  position: fixed;
  top: 0;
}

/* 
3. **`padding`, `margin`, এবং `border` প্রপার্টি কিভাবে কাজ করে?**

**উত্তর:** 
- `padding`: এলিমেন্টের ভেতরের কনটেন্ট এবং সীমানার মধ্যে স্থান।
- `margin`: বাইরের স্থান।
- `border`: সীমানার আকার।

**উদাহরণ:*/

.box {
  padding: 10px;
  margin: 15px;
  border: 1px solid black;
}

/* 
4. **`flex` প্রপার্টি কী? `flex-grow`, `flex-shrink`, এবং `flex-basis` কীভাবে কাজ করে?**

**উত্তর:** `flex` প্রপার্টি দিয়ে ফ্লেক্স আইটেমের সাইজ সেট করা হয়:
- `flex-grow`: স্পেস ভাগ করা।
- `flex-shrink`: সংকোচন কন্ট্রোল।
- `flex-basis`: আইটেমের প্রাথমিক সাইজ।

**উদাহরণ:*/

.item {
  flex: 1;
  flex-grow: 2;
  flex-shrink: 1;
  flex-basis: 100px;
}

/* 
5. **`width`, `min-width`, এবং `max-width` এর মধ্যে পার্থক্য কী?**

**উত্তর:** 
- `width`: এলিমেন্টের প্রস্থ সেট করে।
- `min-width`: সর্বনিম্ন প্রস্থ সেট করে।
- `max-width`: সর্বোচ্চ প্রস্থ নির্ধারণ করে।

**উদাহরণ:*/

.container {
  width: 100%;
  min-width: 300px;
  max-width: 1200px;
}

/* 
6. **`z-index` প্রপার্টি কীভাবে কাজ করে এবং কখন এটি ব্যবহার করা হয়?**

**উত্তর:** `z-index` এলিমেন্টগুলোর স্তর নির্ধারণে ব্যবহৃত হয়। উচ্চ `z-index` মান দিয়ে উপরের স্তরে এলিমেন্ট প্রদর্শিত হয়।

**উদাহরণ:*/

.overlay {
  position: absolute;
  z-index: 10;
}

/* 
7. **`opacity` এবং `visibility` প্রপার্টির মধ্যে পার্থক্য কী?**

**উত্তর:** 
- `opacity`: এলিমেন্টের স্বচ্ছতার মাত্রা নির্ধারণ করে, 0 মানে সম্পূর্ণ অদৃশ্য।
- `visibility`: এলিমেন্ট দৃশ্যমান বা অদৃশ্য রাখে।

**উদাহরণ:*/

.hidden-element {
  opacity: 0.5;
  visibility: hidden;
}


/* thired section */

/*
1. **`flexbox` আর `CSS Grid` এর মধ্যে পার্থক্য কি, এবং কখন কোনটা ব্যবহার করা উচিত?**

**উত্তর:** 
- **Flexbox**: এক-মাত্রিক লেআউট সিস্টেম, সাধারণত উপাদানগুলোকে একটি সারি বা কলামে সাজানোর জন্য ব্যবহৃত হয়। নেভিগেশন বার বা আইটেমগুলিকে অনুভূমিক বা উল্লম্বভাবে সাজাতে উপযুক্ত।
- **CSS Grid**: দুই-মাত্রিক লেআউট সিস্টেম যা সারি এবং কলামের উপর নিয়ন্ত্রণ দিতে পারে, তাই জটিল লেআউট ডিজাইন করার জন্য সেরা।

**উদাহরণ**:
.container-flex {
  display: flex;
}

.container-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}
*/

/*
2. **মিডিয়া কুয়েরি ব্যবহার করে কিভাবে একটি রেসপন্সিভ লেআউট তৈরি করবেন?**

**উত্তর:** মিডিয়া কুয়েরি CSS কে ডিভাইসের স্ক্রিন প্রপার্টি অনুযায়ী স্টাইল পরিবর্তন করতে দেয়, যা ডেক্সটপ, ট্যাবলেট এবং মোবাইলের জন্য আলাদা লেআউট প্রয়োগের ক্ষেত্রে কার্যকর।

**উদাহরণ**:
@media (max-width: 768px) {
  .container {
    flex-direction: column;
  }
}
*/

/*
3. **CSS এ কিভাবে একটি আইটেমকে কেন্দ্রীভূত করবেন? বিভিন্ন পদ্ধতি দিন।**

**উত্তর:** কেন্দ্রীভূত করার কয়েকটি পদ্ধতি রয়েছে:
- ব্লক উপাদানের জন্য `margin: auto` ব্যবহার করা।
- `justify-content` এবং `align-items` দিয়ে `flexbox` ব্যবহার করা।
- `place-items: center` দিয়ে `grid` ব্যবহার করা।

**উদাহরণ**:
.center-flex {
  display: flex;
  justify-content: center;
  align-items: center;
}
*/


/* fourth section */

/* 1. Universal Selector কী? */
/* Universal selector * মানে সব element কে select করে। এটা দিয়ে আমরা সব element এর উপর একসাথে style apply করতে পারি। 
   উদাহরণ: */
   * {
    color: blue; /* এই কোডে সব element এর text color blue করা হবে */
  }
  
  /* 2. CSS Preprocessors কী এবং কিভাবে কাজ করে? SASS বা LESS এর মত CSS preprocessors
     আমাদের CSS কে dynamic করতে সাহায্য করে। এগুলোর মাধ্যমে আমরা variables, functions, mixins 
     ব্যবহার করতে পারি। SASS এবং LESS কোডকে CSS ফাইলে convert করে দেয়। 
     উদাহরণ: */
  $primary-color: #333; /* SASS variable */
  button {
    background-color: $primary-color; /* এটা দিয়ে আমরা $primary-color ব্যবহার করে button এর background-color সেট করতে পারি */
  }
  
  /* 3. CSS Custom Properties (CSS Variables) কী এবং এর সুবিধা কী? */
  /* CSS variables আমাদের জন্য একটা value সংরক্ষণ করে রাখে যেটি পরবর্তীতে অন্য property গুলোতে ব্যবহার করা যায়। 
     এর ফলে reusability এবং maintainability বৃদ্ধি পায়। 
     উদাহরণ: */
  :root {
    --main-bg-color: coral; /* CSS variable */
  }
  body {
    background-color: var(--main-bg-color); /* CSS variable ব্যবহার করা হয়েছে */
  }
  
  /* 4. CSS Animations কিভাবে কাজ করে এবং keyframe animation কিভাবে তৈরি করা যায়? */
  /* CSS animations বড় animation task এ ব্যবহৃত হয়। Keyframes দিয়ে animation নির্ধারণ করা হয়। */
  /* উদাহরণ: */
  @keyframes slide {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(100px);
    }
  }
  div {
    animation: slide 2s ease-in-out; /* animation টি apply করা হয়েছে */
  }
  
  /* 5. CSS Transitions কী এবং animations এর সাথে এর পার্থক্য কী? */
  /* CSS transitions দিয়ে আমরা element এর state change করার সময় smooth transition পেতে পারি। 
     Animations তুলনায় transitions ছোট এবং নির্দিষ্ট পরিবর্তন করে। উদাহরণ: */
  div {
    transition: background-color 0.3s ease;
  }
  div:hover {
    background-color: red; /* hover করার পর background-color smoothly change হবে */
  
  /* 6. `rem`, `em`, এবং `px` unit এর মধ্যে কী পার্থক্য? */
  /* `px` একটি fixed unit, যার size fixed থাকে। `em` এবং `rem` relative unit, যেখানে `rem` root element এর size কে refer করে, 
     আর `em` parent element এর size কে refer করে। */
  html {
    font-size: 16px;
  }
  div {
    font-size: 2rem; /* 32px হবে */
  }
  
  /* 7. SVG কী এবং কিভাবে custom shapes তৈরি করা যায়? */
  /* SVG (Scalable Vector Graphics) একটি vector-based image format যা কোনো resolution এ স্পষ্ট থাকে। 
     Custom shapes তৈরি করা যায় shape, path, circle এর মাধ্যমে। */
  svg {
    width: 100px;
    height: 100px;
  }
  circle {
    cx: 50;
    cy: 50;
    r: 40;
    fill: red;
  } /* এই উদাহরণে একটি circle SVG image তৈরি করা হয়েছে */
  








  <!-- ----------------------------------------bootstap----------------------------- -->
  What is Bootstrap, and why is it used in web development? Bootstrap হলো একটি ওপেন সোর্স ফ্রন্ট-এন্ড ফ্রেমওয়ার্ক, যা ওয়েবসাইট ডিজাইনকে আরও দ্রুত এবং সহজ করে তোলে। এটি CSS, JavaScript, এবং কম্পোনেন্টের একটি সংগ্রহ, যার সাহায্যে রেসপন্সিভ এবং মোবাইল ফার্স্ট ওয়েবসাইট ডিজাইন করা যায়। এর মধ্যে নানা ধরনের UI কম্পোনেন্ট যেমন ন্যাভবার, বোতাম, ফর্ম, মডাল ইত্যাদি অন্তর্ভুক্ত রয়েছে।
How do you include Bootstrap in a project? Describe the different methods. আপনি তিনটি উপায়ে Bootstrap প্রজেক্টে অন্তর্ভুক্ত করতে পারেন:
CDN (Content Delivery Network): সরাসরি HTML ফাইলের <head> ট্যাগে CSS এবং <body> ট্যাগের শেষের দিকে JavaScript যোগ করে।
লোকালি ডাউনলোড করা: Bootstrap ফাইলগুলো ডাউনলোড করে প্রজেক্টের মধ্যে যুক্ত করা।
NPM অথবা Yarn দিয়ে: Node Package Manager (npm) বা Yarn ব্যবহার করে Bootstrap ইনস্টল করা।
Explain the grid system in Bootstrap. How many columns does it have, and how does it work? Bootstrap এর গ্রিড সিস্টেম ১২টি সমান কলামে বিভক্ত। আপনি কাস্টমাইজড লেআউট তৈরি করতে একাধিক কলাম ব্যবহার করতে পারেন। উদাহরণস্বরূপ, col-4 মানে ১২টি কলামের মধ্যে ৪টি কলাম ব্যবহার করা হবে। এবং আপনি বিভিন্ন ব্রেকপয়েন্টের জন্য আলাদা কলাম সাইজ ব্যবহার করতে পারেন, যেমন col-sm, col-md ইত্যাদি।
What are Bootstrap containers, and what’s the difference between .container and .container-fluid?
.container: এটি একটি স্থির আকারের কনটেইনার, যেটি বিভিন্ন স্ক্রীন সাইজ অনুযায়ী রেসপন্সিভ হয়।
.container-fluid: এটি পুরোপুরি স্ক্রীনের প্রস্থে বিস্তৃত, অর্থাৎ এটি সবসময় সম্পূর্ণ প্রস্থ নেয়।
What are Bootstrap classes, and how do they differ from CSS classes? Bootstrap ক্লাসগুলো হলো প্রি-ডিফাইন্ড ক্লাস, যেগুলো একটি নির্দিষ্ট স্টাইলিং প্রভাব তৈরি করে, যেমন .btn, .navbar, .container ইত্যাদি। CSS ক্লাসগুলো হলো আপনার নিজস্ব ক্লাস, যেগুলো আপনি প্রজেক্টে ডিজাইন অনুযায়ী তৈরি করে থাকেন।
Explain the concept of responsive design in Bootstrap. রেসপন্সিভ ডিজাইন হলো এমন একটি ডিজাইন কৌশল, যা ওয়েবসাইটকে সব ধরনের স্ক্রীনে ভালভাবে দেখানোর জন্য বানানো হয়। Bootstrap গ্রিড সিস্টেম, মিডিয়া কোয়েরি এবং প্রি-ডিফাইন্ড ক্লাসগুলো ব্যবহার করে বিভিন্ন স্ক্রীন সাইজের জন্য কাস্টম ডিজাইন তৈরি করতে সাহায্য করে।
How do breakpoints work in Bootstrap? List some commonly used breakpoints. ব্রেকপয়েন্ট হলো ওয়েবসাইটের সেই অংশ, যেখানে স্ক্রীন সাইজের ভিত্তিতে লেআউট পরিবর্তিত হয়। Bootstrap এ কিছু কমন ব্রেকপয়েন্ট হলো:
xs: <576px
sm: ≥576px
md: ≥768px
lg: ≥992px
xl: ≥1200px
What is a Bootstrap component? Give examples of some commonly used components. Bootstrap কম্পোনেন্টগুলো হলো প্রি-বিল্ট UI এলিমেন্ট যেগুলো ওয়েবসাইটে দ্রুত ব্যবহার করা যায়। উদাহরণস্বরূপ:
Navbar: ন্যাভিগেশন বার
Card: তথ্য প্রদর্শনের জন্য একটি কন্টেইনার
Modal: পপ-আপ ডায়ালগ
Alert: বার্তা প্রদর্শন (সাকসেস, ডেঞ্জার ইত্যাদি)
How do you create a navigation bar using Bootstrap? Explain any commonly used classes. ন্যাভবার তৈরি করতে Bootstrap এর .navbar, .navbar-light, .navbar-expand-lg ইত্যাদি ক্লাস ব্যবহার করা হয়। এটি একটি রেসপন্সিভ ন্যাভিগেশন বার তৈরি করতে সাহায্য করে।
What is a card in Bootstrap, and how do you create one? Card হলো একটি কন্টেইনার, যা বিভিন্ন ধরনের কন্টেন্ট যেমন ইমেজ, টেক্সট, বাটন ইত্যাদি ধারণ করতে পারে। এটি তৈরি করতে .card ক্লাস ব্যবহার করা হয়।
html
Copy code
<div class="card" style="width: 18rem;">
  <img src="image.jpg" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">Card Title</h5>
    <p class="card-text">Some quick example text to build on the card title.</p>
    <a href="#" class="btn btn-primary">Go somewhere</a>
  </div>
</div>

Explain how the modal component works in Bootstrap. How do you trigger a modal? Modal হলো একটি পপ-আপ ডায়ালগ বক্স, যা কোনো ব্যবহারকারীর ইন্টারঅ্যাকশনের মাধ্যমে ট্রিগার হয়। আপনি একটি বোতাম ক্লিক করলে মডাল প্রদর্শিত হয়। উদাহরণস্বরূপ, একটি modal তৈরি করা যেতে পারে এবং data-toggle="modal" এবং data-target="#myModal" ব্যবহার করে এটি ট্রিগার করা হয়।

















<!-- --------------------------------------------tailwind css -------------------------- -->


টেইলউইন্ড CSS কী, এবং এটি কীভাবে Bootstrap-এর মতো ঐতিহ্যগত CSS ফ্রেমওয়ার্ক থেকে আলাদা?
টেইলউইন্ড CSS একটি ইউটিলিটি-ফার্স্ট CSS ফ্রেমওয়ার্ক, যা প্রস্তুত স্টাইল রেঞ্জ দিয়ে তৈরি UI উপাদান গঠন করে। Bootstrap যেখানে প্রস্তুত করা কম্পোনেন্ট দিয়ে কাজ করে, সেখানে টেইলউইন্ড আপনাকে শুধুমাত্র প্রয়োজনীয় CSS ক্লাস দেয়, যাতে আপনি আপনার UI কাস্টমাইজডভাবে ডিজাইন করতে পারেন।
টেইলউইন্ড CSS প্রজেক্টে কীভাবে সেটআপ করবেন? বিভিন্ন ইনস্টলেশন পদ্ধতি বর্ণনা করুন।
টেইলউইন্ড CSS প্রজেক্টে ইনস্টল করার তিনটি প্রধান উপায়:
CDN ব্যবহার: সরাসরি <head> ট্যাগে টেইলউইন্ডের CSS লিংক দিয়ে।
npm/yarn দিয়ে: npm install tailwindcss ব্যবহার করে।
PostCSS ব্যবহার: PostCSS এবং Tailwind CSS কনফিগারেশন ফাইল দিয়ে ইনস্টলেশন।
টেইলউইন্ড CSS-এর ইউটিলিটি ক্লাস কী? এগুলি কিভাবে UI কম্পোনেন্ট তৈরি করতে সাহায্য করে?
ইউটিলিটি ক্লাস হল একক-purpose CSS ক্লাস, যেমন bg-red-500, text-lg, p-4, যা ডিজাইনের জন্য স্টাইল দেয়। এগুলি কোডের মধ্যে কমপ্লেক্স স্টাইলিং অল্প সময়ের মধ্যে প্রয়োগ করতে সাহায্য করে।
টেইলউইন্ড CSS এর JIT (Just-in-Time) মোডের সুবিধা কী?
JIT মোডের মাধ্যমে টেইলউইন্ড শুধু ব্যবহৃত ক্লাসগুলোকে প্রোডাকশনে প্রোসেস করে, যার ফলে CSS ফাইলের সাইজ ছোট হয় এবং উন্নত পারফরম্যান্স পাওয়া যায়।
টেইলউইন্ডের কনফিগারেশন ফাইল কী, এবং এগুলি কিভাবে ফ্রেমওয়ার্ক কাস্টমাইজ করে?
কনফিগারেশন ফাইল (tailwind.config.js) ব্যবহার করে আপনি টেইলউইন্ডের থিম, রং, স্পেসিং, ব্রেকপয়েন্ট ইত্যাদি কাস্টমাইজ করতে পারেন।
@apply ডিরেকটিভ কীভাবে কাজ করে, এবং এর উদ্দেশ্য কী?
@apply ডিরেকটিভ আপনাকে টেইলউইন্ডের ইউটিলিটি ক্লাসকে CSS স্টাইলশিটে একত্রিত করার সুযোগ দেয়, যাতে আপনি কাস্টম ক্লাসে একই স্টাইল একত্রিত করতে পারেন।
টেইলউইন্ডে রেসপন্সিভ ডিজাইন ইউটিলিটি কী, এবং ব্রেকপয়েন্ট কীভাবে কাজ করে?
টেইলউইন্ড রেসপন্সিভ ইউটিলিটি ব্রেকপয়েন্টের মাধ্যমে স্টাইল শর্তে প্রয়োগ করে, যেমন sm, md, lg, xl, এগুলি বিভিন্ন স্ক্রীন সাইজের জন্য আলাদা স্টাইল প্রয়োগ করতে ব্যবহার করা হয়।
টেইলউইন্ড কিভাবে থিমিং এবং কাস্টম রং পরিচালনা করে?
টেইলউইন্ড কনফিগারেশন ফাইলে কাস্টম রং এবং থিম সেট করা যেতে পারে, যেমন colors সেকশনে নতুন রঙ যোগ করা।
টেইলউইন্ড ইউটিলিটি সম্পর্কে সাধারণ প্রশ্ন (৭টি প্রশ্ন)
টেইলউইন্ডের স্পেসিং ইউটিলিটি (যেমন m-, p-) কিভাবে কাজ করে?
স্পেসিং ইউটিলিটি ক্লাস যেমন m-4 (margin), p-4 (padding) আপনার এলিমেন্টে মার্জিন বা প্যাডিং প্রয়োগ করতে সাহায্য করে।
টেইলউইন্ডে ফ্লেক্সবক্স ইউটিলিটি কীভাবে লেআউট তৈরি করতে ব্যবহার করবেন?
টেইলউইন্ডে flex, justify-between, items-center ইত্যাদি ক্লাস ব্যবহার করে ফ্লেক্সবক্স লেআউট তৈরি করা হয়।
টেইলউইন্ডে টাইপোগ্রাফি ইউটিলিটি কী, এবং আপনি কিভাবে টেক্সট স্টাইল করবেন?
টাইপোগ্রাফি ইউটিলিটি ক্লাস, যেমন text-lg, font-bold, text-center, ব্যবহার করে আপনি সহজে টেক্সট সাইজ, ফন্ট-weight, এলাইনমেন্ট কাস্টমাইজ করতে পারেন।
টেইলউইন্ডে ব্যাকগ্রাউন্ড এবং রং ইউটিলিটি কীভাবে কাজ করে?
ব্যাকগ্রাউন্ড এবং রং ইউটিলিটি ক্লাস যেমন bg-blue-500, text-white, bg-gradient-to-r ব্যবহার করে ব্যাকগ্রাউন্ড রঙ এবং টেক্সট রঙ সেট করা হয়।
টেইলউইন্ডে স্টেট ভেরিয়েন্ট কী, এবং এগুলি হোভার, ফোকাস ইত্যাদি কীভাবে প্রয়োগ হয়?
স্টেট ভেরিয়েন্ট, যেমন hover:bg-blue-500, focus:outline-none ব্যবহার করে বিভিন্ন ইন্টারঅ্যাকশন (হোভার, ফোকাস) অবস্থায় স্টাইল প্রয়োগ করা হয়।
টেইলউইন্ডে ফন্ট কাস্টমাইজেশন কিভাবে করবেন?
কনফিগারেশন ফাইলে fontFamily এবং fontSize কাস্টমাইজ করা যায়, এছাড়া text-xl, font-sans ক্লাস দিয়ে দ্রুত ফন্ট সাইজ এবং ফন্ট ফ্যামিলি কাস্টমাইজ করা হয়।
টেইলউইন্ডের গ্রিড ইউটিলিটি কীভাবে কাজ করে, এবং এটি CSS Grid এর সাথে কিভাবে তুলনা করা যায়?
টেইলউইন্ডে গ্রিড ইউটিলিটি যেমন grid-cols-3, grid-rows-2 ব্যবহার করে একাধিক কলাম এবং রো তৈরি করা যায়। এটি CSS Grid এর মতো, তবে টেইলউইন্ড ক্লাসগুলো আরও সহজ এবং ফাস্ট।
টেইলউইন্ড লেআউট এবং রেসপন্সিভ ডিজাইন (৮টি প্রশ্ন)
ফ্লেক্সবক্স এবং গ্রিড ইউটিলিটিগুলোর মধ্যে পার্থক্য কী, এবং কখন আপনি কোনটি ব্যবহার করবেন?
ফ্লেক্সবক্স একমাত্র এক্স-অ্যাক্সিস (row বা column) লেআউটের জন্য ব্যবহার হয়, যখন CSS Grid ২D লেআউট (row এবং column) জন্য উপযুক্ত।
টেইলউইন্ডে রেসপন্সিভ লেআউট কিভাবে তৈরি করবেন? উদাহরণ দিন।
টেইলউইন্ডের মাধ্যমে আপনি sm, md, lg, xl ক্লাস ব্যবহার করে রেসপন্সিভ লেআউট তৈরি করতে পারেন, যেমন:
html
Copy code
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3">
  <!-- আপনার কন্টেন্ট -->
</div>


টেইলউইন্ডে এলিমেন্ট সেন্টার কিভাবে করবেন? বিভিন্ন পদ্ধতি তালিকাভুক্ত করুন।
flex justify-center items-center, mx-auto, এবং text-center ব্যবহার করে এলিমেন্ট সেন্টার করা যায়।
টেইলউইন্ডের container ক্লাস কী, এবং এটি কিভাবে লেআউটের সাহায্য করে?
container ক্লাস একটি প্রিসেট উইডথ সেট করে, যাতে আপনার কনটেন্ট বিভিন্ন স্ক্রীনে সেন্টার হয়।
টেইলউইন্ডে স্ক্রীন সাইজ মডিফায়ার কীভাবে কাজ করে এবং কিছু সাধারণ ব্রেকপয়েন্ট তালিকাভুক্ত করুন।
টেইলউইন্ডে স্ক্রীন সাইজ মডিফায়ার যেমন sm:, md:, lg: ব্যবহার করে বিভিন্ন সাইজের জন্য স্টাইলিং প্রয়োগ করা হয়।
টেইলউইন্ডে ফিক্সড এবং স্টিকি এলিমেন্ট কিভাবে তৈরি করবেন?
fixed, sticky ক্লাস ব্যবহার করে ফিক্সড এবং স্টিকি এলিমেন্ট তৈরি করা হয়।
টেইলউইন্ডে রেসপন্সিভ হাইডিং/শোইং ইউটিলিটি কী, এবং এগুলি কীভাবে কাজ করে?
hidden, block, lg:hidden এর মতো ক্লাস ব্যবহার করে আপনি নির্দিষ্ট স্ক্রীন সাইজের জন্য এলিমেন্টগুলো হাইড বা শো করতে পারেন।
টেইলউইন্ড মোবাইল-ফার্স্ট ডিজাইন কিভাবে সমর্থন করে?
টেইলউইন্ডের সমস্ত ইউটিলিটি মোবাইল-









<!-- ----------------------------------------javascript ------------------------------- -->
// **JavaScript Fundamentals**

/* 
1. What are the different data types in JavaScript? 
JavaScript এর data types: 
- Primitive: string, number, boolean, null, undefined, symbol, bigint
- Non-Primitive: objects (like arrays, functions, etc.)
*/

// Example:
let stringType = "Hello"; // string
let numberType = 123; // number
let booleanType = true; // boolean
let nullType = null; // null
let undefinedType; // undefined

/*
2. What is the difference between `var`, `let`, and `const`?
- `var`: Function-scoped, allows re-declaration, can be hoisted.
- `let`: Block-scoped, no re-declaration, can be reassigned.
- `const`: Block-scoped, cannot be reassigned or re-declared.
*/

// Example:
var varExample = "I'm var"; // function scope
let letExample = "I'm let"; // block scope
const constExample = "I'm const"; // block scope, constant value

/*
3. Explain JavaScript's `==` vs. `===` operators.
- `==`: Loose equality, performs type coercion.
- `===`: Strict equality, does not perform type coercion.
*/

// Example:
console.log(5 == "5"); // true (type coercion)
console.log(5 === "5"); // false (no type coercion)

/*
4. What is type coercion in JavaScript? Give an example.
Type coercion: JavaScript automatically converts data types when needed.
*/

// Example:
console.log("5" + 5); // "55" (number coerced to string)
console.log("5" - 2); // 3 (string coerced to number)

/*
5. Explain the concept of scope in JavaScript.
Scope determines where variables are accessible.
- Block scope: `let` and `const`
- Function scope: `var`
*/

// Example:
function scopeExample() {
  let x = 10; // block-scoped
  if (true) {
    var y = 20; // function-scoped
  }
  console.log(y); // Accessible
  // console.log(x); // Not accessible
}

/*
6. What is hoisting in JavaScript?
Hoisting: Variables and function declarations are moved to the top of their scope.
*/

// Example:
console.log(hoistedVar); // undefined (hoisted)
var hoistedVar = "I am hoisted";

/*
7. What are template literals, and how are they used?
Template literals: Strings enclosed by backticks ``. They allow embedded expressions.
*/

// Example:
const name = "Yeasin";
console.log(`Hello, ${name}!`); // "Hello, Yeasin!"

/*
8. Explain what a higher-order function is in JavaScript.
A higher-order function takes another function as an argument or returns a function.
*/

// Example:
function higherOrder(func) {
  return func();
}
higherOrder(() => console.log("I'm a higher-order function"));

/*
9. What are arrow functions, and how are they different from regular functions?
Arrow functions have a concise syntax and do not have their own `this`.
*/

// Example:
const arrowFunction = () => console.log("I am an arrow function");

/*
10. What is an Immediately Invoked Function Expression (IIFE)?
An IIFE is a function that runs immediately after it's defined.
*/

// Example:
(function () {
  console.log("I am an IIFE!");
})();

// **Functions and Objects**

/*
11. Explain the concept of closures in JavaScript.
A closure is a function that has access to its outer function's scope even after the function has closed.
*/

// Example:
function closureExample() {
  let count = 0;
  return function () {
    count++;
    return count;
  };
}
const increment = closureExample();
console.log(increment()); // 1
console.log(increment()); // 2

/*
12. What is the `this` keyword, and how does it behave in different contexts?
`this` refers to the object in context.
- In global scope: `this` refers to the global object.
- In object methods: `this` refers to the object itself.
- In arrow functions: `this` is lexically bound.
*/

// Example:
const obj = {
  name: "Yeasin",
  greet: function () {
    console.log(this.name); // "Yeasin"
  },
};
obj.greet();

/*
13. How do you create an object in JavaScript?
Objects can be created using object literals or the `new` keyword.
*/

// Example:
const myObject = { key: "value" };
const anotherObject = new Object();

/*
14. What is the difference between `null` and `undefined`?
- `null`: Intentional absence of value.
- `undefined`: Variable declared but not assigned a value.
*/

// Example:
let nullValue = null; // intentionally empty
let undefinedValue; // automatically undefined

/*
15. How do you copy an object in JavaScript? Explain shallow vs. deep copy.
- Shallow copy: Copies the first level.
- Deep copy: Copies all levels recursively.
*/

// Example:
const obj1 = { a: 1, b: { c: 2 } };
const shallowCopy = { ...obj1 }; // shallow copy
const deepCopy = JSON.parse(JSON.stringify(obj1)); // deep copy

/*
16. Explain how `call`, `apply`, and `bind` work in JavaScript.
- `call`: Invokes a function with a given `this` value and arguments.
- `apply`: Similar to `call` but takes arguments as an array.
- `bind`: Returns a new function with `this` bound.
*/

// Example:
function greet(greeting) {
  console.log(`${greeting}, ${this.name}`);
}
const person = { name: "Yeasin" };
greet.call(person, "Hello"); // "Hello, Yeasin"
greet.apply(person, ["Hi"]); // "Hi, Yeasin"
const boundGreet = greet.bind(person);
boundGreet("Hey"); // "Hey, Yeasin"

/*
17. What is the prototype chain, and how does inheritance work in JavaScript?
The prototype chain is a mechanism for inheritance. Objects inherit from other objects.
*/

// Example:
function Animal(name) {
  this.name = name;
}
Animal.prototype.speak = function () {
  console.log(`${this.name} makes a sound`);
};
const dog = new Animal("Dog");
dog.speak(); // "Dog makes a sound"

// **Arrays and Strings**

/*
18. What are some common array methods in JavaScript?
- `push()`, `pop()`, `shift()`, `unshift()`, `map()`, `filter()`, `reduce()`, `forEach()`
*/

// Example:
const arr = [1, 2, 3];
arr.push(4); // [1, 2, 3, 4]

/*
19. How does `map()` differ from `forEach()` in arrays?
- `map()`: Returns a new array.
- `forEach()`: Iterates over an array but does not return a new array.
*/

// Example:
const mappedArr = arr.map((num) => num * 2); // [2, 4, 6]
arr.forEach((num) => console.log(num)); // Logs each element

/*
20. Explain the `filter()` method. How does it work?
`filter()` creates a new array with elements that pass a condition.
*/

// Example:
const filteredArr = arr.filter((num) => num > 2); // [3, 4]

/*
21. What does the `reduce()` method do, and how is it used?
`reduce()` applies a function to accumulate array elements into a single value.
*/

// Example:
const sum = arr.reduce((acc, num) => acc + num, 0); // 10

/*
22. How do you find the length of a string and reverse it?
- Length: `str.length`
- Reverse: Use array methods.
*/

// Example:
const str = "Hello";
console.log(str.length); // 5
console.log(str.split("").reverse().join("")); // "olleH"

/*
23. What are template literals, and how can they be used for string manipulation?
Template literals allow for string interpolation and multi-line strings.
*/

// Example:
const multilineString = `This is a
multi-line string`;
console.log(`My name is ${name}`); // "My name is Yeasin"

/*
24. How do you remove duplicates from an array?
Use `Set` or `filter()`.
*/

// Example:
const nums = [1, 2, 2, 3];
const uniqueNums = [...new Set(nums)]; // [1, 2, 3]

// **Control Structures and Error Handling**

/*
25. How does JavaScript handle implicit type conversion?
JavaScript converts types automatically, such as in mathematical operations.
*/

// Example:
console.log("5" - 2); // 3

/*
26. What does `typeof` return for different data types?
Returns a string indicating the data type.
*/

// Example:
console.log(typeof 123); // "number"
console.log(typeof "Hello"); // "string"

/*
27. What is NaN, and how can you check if a value is NaN?
NaN stands for "Not-A-Number". Use `isNaN()` to check.
*/

// Example:
console.log(isNaN("abc")); // true


// **Miscellaneous**

/*
28. What is event delegation, and how does it work?
Event delegation is a technique where you use a single event listener to manage events for multiple elements, especially useful when elements are dynamically added. Events bubble up to a common ancestor, where you can handle them efficiently.
*/

// Example:
document.querySelector("#parent").addEventListener("click", (event) => {
    if (event.target && event.target.matches("button.className")) {
      console.log("Button clicked!");
    }
  });
  
  /*
  29. What are default parameters in JavaScript?
  Default parameters allow functions to use default values if no arguments are provided.
  */
  
  // Example:
  function greet(name = "Guest") {
    console.log(`Hello, ${name}!`);
  }
  greet(); // "Hello, Guest!"
  greet("Yeasin"); // "Hello, Yeasin!"
  
  /*
  30. What is the difference between synchronous and asynchronous programming?
  - Synchronous: Code runs line by line, and each statement waits for the previous one to finish.
  - Asynchronous: Code runs in the background and does not block the execution of subsequent statements.
  */
  
  // Example:
  // Synchronous:
  console.log("Start");
  console.log("End");
  
  // Asynchronous:
  console.log("Start");
  setTimeout(() => console.log("Delayed"), 1000); // Runs after 1 second
  console.log("End");
  
  /*
  31. How does the `setTimeout` function work, and what is its use?
  `setTimeout` is used to execute a function after a specified delay (in milliseconds). It is commonly used for delayed or deferred execution.
  */
  
  // Example:
  setTimeout(() => {
    console.log("Executed after 2 seconds");
  }, 2000); // 2-second delay
  
  /*
  32. What is the purpose of `JSON.stringify()` and `JSON.parse()`?
  - `JSON.stringify()`: Converts a JavaScript object to a JSON string.
  - `JSON.parse()`: Converts a JSON string back to a JavaScript object.
  */
  
  // Example:
  const obj = { name: "Yeasin", age: 20 };
  const jsonString = JSON.stringify(obj); // '{"name":"Yeasin","age":20}'
  const parsedObj = JSON.parse(jsonString); // { name: "Yeasin", age: 20 }
  
  /*
  33. How can you handle asynchronous code in JavaScript?
  Asynchronous code can be handled using callbacks, promises, or async/await.
  */
  
  // Example:
  // Using Promises:
  fetch("https://api.example.com/data")
    .then((response) => response.json())
    .then((data) => console.log(data))
    .catch((error) => console.error(error));
  
  // Using async/await:
  async function fetchData() {
    try {
      const response = await fetch("https://api.example.com/data");
      const data = await response.json();
      console.log(data);
    } catch (error) {
      console.error(error);
    }
  }
  fetchData();
  
  /*
  34. Explain the concept of the Event Loop in JavaScript.
  The Event Loop is a mechanism that handles the execution of multiple pieces of code, including asynchronous code, in JavaScript. It constantly checks if the call stack is empty and pushes pending tasks from the callback queue (or microtask queue) into the call stack for execution.
  */
  
  // Example:
  console.log("Start");
  setTimeout(() => {
    console.log("Callback");
  }, 0);
  console.log("End");
  // Output: "Start", "End", "Callback" (due to the Event Loop)
  










  <!-- ---------------------------advance javascript ------------------------------ -->
  // **BOM**

/*
1. What is the Browser Object Model (BOM) in JavaScript?
The BOM provides methods and properties to interact with the browser window (e.g., `window`, `navigator`, `screen`).
*/

/*
2. How does `window` differ from `document`?
- `window`: Represents the browser window and provides methods like `alert`, `setTimeout`.
- `document`: Represents the HTML document loaded in the window.
*/

/*
3. Explain how `localStorage`, `sessionStorage`, and `cookies` work and their differences.
- `localStorage`: Stores data with no expiration.
- `sessionStorage`: Stores data for the session duration.
- `cookies`: Stores data with customizable expiration.
*/

// Project: Store user theme in `localStorage`
localStorage.setItem("theme", "dark");
console.log(localStorage.getItem("theme")); // "dark"

/*
4. What is the purpose of the `navigator` object, and what properties does it have?
The `navigator` object provides information about the browser (e.g., `userAgent`, `language`).
*/

/*
5. How do `window.open` and `window.close` methods work in JavaScript?
- `window.open`: Opens a new browser window.
- `window.close`: Closes the current window.
*/

// Example:
const newWindow = window.open("https://example.com", "_blank");
newWindow.close();

/*
6. Explain how to get the viewport width and height of a browser window using JavaScript.
*/

// Example:
console.log(`Width: ${window.innerWidth}, Height: ${window.innerHeight}`);

// Project: Log window size on resize
window.addEventListener("resize", () => {
  console.log(`Resized to: ${window.innerWidth}x${window.innerHeight}`);
});

/*
7. What is the purpose of the `setTimeout` and `setInterval` functions?
- `setTimeout`: Executes a function after a delay.
- `setInterval`: Repeatedly executes a function at specified intervals.
*/

// Project: Clock using `setInterval`
setInterval(() => {
  console.log(new Date().toLocaleTimeString());
}, 1000);


// **BOM**

/*
8. How can you detect if a user is online or offline using the BOM?
You can use the `navigator.onLine` property to check if the user is online or offline and listen for the `online` and `offline` events to respond to changes in connectivity.
*/

// Example:
if (navigator.onLine) {
    console.log("You are online!");
  } else {
    console.log("You are offline!");
  }
  
  // Event listeners for connectivity changes
  window.addEventListener("online", () => {
    console.log("Connection restored!");
  });
  window.addEventListener("offline", () => {
    console.log("You are now offline!");
  });
  
  // **Web API & JSON**
  
  /*
  1. What is the Fetch API, and how does it work with Promises?
  The Fetch API provides an easy way to make HTTP requests. It returns a Promise that resolves to the Response object, which can be parsed to get the data.
  */
  
  // Project: Fetch data from JSONPlaceholder and display it
  fetch("https://jsonplaceholder.typicode.com/posts")
    .then((response) => response.json())
    .then((data) => {
      console.log(data); // Display the fetched data
    })
    .catch((error) => console.error("Error fetching data:", error));
  
  /*
  2. How does `XMLHttpRequest` differ from the Fetch API?
  - `XMLHttpRequest`: Older way to make HTTP requests, more complex syntax, supports callbacks.
  - `Fetch API`: Modern, simpler syntax, uses Promises, built-in JSON support.
  */
  
  /*
  3. Explain what JSON is and how it’s used in JavaScript.
  JSON (JavaScript Object Notation) is a format for structuring data, commonly used for exchanging data between a server and a client.
  */
  
  /*
  4. How do you parse JSON data in JavaScript, and how do you stringify JavaScript objects?
  - Parsing: Use `JSON.parse()` to convert a JSON string into a JavaScript object.
  - Stringifying: Use `JSON.stringify()` to convert a JavaScript object into a JSON string.
  */
  
  // Project: Convert object to JSON and back
  const user = { name: "Yeasin", age: 20 };
  const jsonString = JSON.stringify(user);
  console.log(jsonString); // '{"name":"Yeasin","age":20}'
  
  const parsedObject = JSON.parse(jsonString);
  console.log(parsedObject); // { name: "Yeasin", age: 20 }
  
  /*
  5. What is CORS, and why do we need it when making API requests?
  CORS (Cross-Origin Resource Sharing) is a security feature that allows or restricts requests made from one origin to a different domain. It prevents unauthorized cross-origin requests.
  */
  
  /*
  6. Explain how the `FormData` API works and when you’d use it.
  The `FormData` API provides a way to easily construct a set of key-value pairs to send form data, typically used for uploading files.
  */
  
  // Example:
  const formData = new FormData();
  formData.append("username", "Yeasin");
  formData.append("profilePic", fileInput.files[0]); // Example for file upload
  
  /*
  7. What is the purpose of the `History` API, and how do `pushState` and `replaceState` work?
  The `History` API lets you manage the browser's history stack. 
  - `pushState`: Adds a new history entry.
  - `replaceState`: Replaces the current history entry.
  */
  
  // Project: Simple single-page navigation
  document.querySelectorAll("a").forEach((link) => {
    link.addEventListener("click", (event) => {
      event.preventDefault();
      const url = event.target.getAttribute("href");
      history.pushState({}, "", url);
      console.log(`Navigated to ${url}`);
    });
  });
  
  /*
  8. How can you handle errors in API requests with `try/catch` blocks and the `.catch()` method?
  - `try/catch`: Handles errors in `async/await` syntax.
  - `.catch()`: Handles errors in Promises.
  */
  
  // Example:
  async function fetchData() {
    try {
      const response = await fetch("https://api.example.com/data");
      const data = await response.json();
      console.log(data);
    } catch (error) {
      console.error("Error fetching data:", error);
    }
  }
  
  /*
  9. What are WebSockets, and how do they differ from HTTP requests?
  WebSockets provide a full-duplex communication channel over a single, long-lived TCP connection, enabling real-time data exchange. Unlike HTTP, WebSockets maintain a persistent connection.
  */
  
  /*
  10. Explain the basics of the Service Worker API and its use in creating progressive web apps (PWAs).
  Service Workers are scripts that run in the background and enable features like offline functionality, push notifications, and caching resources, essential for building PWAs.
  */
  






<!-- ----------------------------------------react js ------------------------------- -->
// 1. **What is React, and why is it used?**
   ```javascript
   import React from 'react';
   import ReactDOM from 'react-dom';

   // React হলো একটি জনপ্রিয় JavaScript লাইব্রেরি যা দ্রুত এবং ইন্টারেক্টিভ UI তৈরি করতে ব্যবহৃত হয়।
   function App() {
     return <h1>Hello, World!</h1>; // Simple একটি UI রেন্ডার করে
   }

   // ReactDOM.render কমান্ডটি JSX ব্যবহার করে HTML element রেন্ডার করে
   ReactDOM.render(<App />, document.getElementById('root'));
   ```

// 2. **What is JSX, and why is it preferred in React?**
   ```javascript
   // JSX একটি JavaScript সিনট্যাক্স এক্সটেনশন, যা কোডকে পরিচ্ছন্ন ও সহজবোধ্য করে তোলে
   const element = <h1>Hello, JSX!</h1>; // HTML-এর মতো দেখালেও এটি JavaScript কোডের অংশ
   ```

// 3. **How does JSX differ from HTML?**
   ```javascript
   // JSX: class এর পরিবর্তে className ব্যবহার করতে হয় এবং JavaScript expressions {} এর মধ্যে লিখতে হয়
   const element = <div className="container">{2 + 2}</div>; // JavaScript expression ব্যবহৃত হয়েছে
   ```

// 4. **What are components in React?**
   ```javascript
   // Components হলো React-এর building blocks, যা পুনরায় ব্যবহারযোগ্য এবং independent UI অংশ তৈরি করে
   function Welcome() {
     return <h1>Welcome to React!</h1>; // Functional component
   }

   class WelcomeClass extends React.Component {
     render() {
       return <h1>Welcome to React!</h1>; // Class component
     }
   }
   ```

5. **What is the difference between functional and class components?**
   ```javascript
   // Functional Component: সাধারণত সহজ এবং React Hooks ব্যবহার করে
   function Greeting(props) {
     return <h1>Hello, {props.name}!</h1>; // Props ব্যবহার করে data pass করা হচ্ছে
   }

   // Class Component: lifecycle methods এবং state ব্যবহারের সুবিধা দেয়
   class GreetingClass extends React.Component {
     render() {
       return <h1>Hello, {this.props.name}!</h1>; // Props এখানে ব্যবহৃত হয়েছে
     }
   }
   ```

// 6. **What is a single-page application (SPA), and how does React enable it?**
//    - SPA এমন একটি ওয়েব অ্যাপ, যেখানে একটি HTML পেজ লোড হয় এবং content dynamically আপডেট হয়। React-এ Virtual DOM এবং client-side routing SPA তৈরি করতে সহায়তা করে।

// 7. **What are props in React?**
   ```javascript
   // Props হলো immutable ডেটা, যা parent কম্পোনেন্ট থেকে child কম্পোনেন্টে data পাস করতে ব্যবহৃত হয়
   function ChildComponent(props) {
     return <p>{props.message}</p>; // Parent থেকে পাস করা message প্রদর্শিত হচ্ছে
   }

   function ParentComponent() {
     return <ChildComponent message="Hello from parent!" />; // Props দ্বারা data পাঠানো হচ্ছে
   }
   ```

// 8. **How is state different from props?**
   ```javascript
   // State হলো কম্পোনেন্টের নিজস্ব data, যা পরিবর্তন করা যায়
   class Counter extends React.Component {
     constructor() {
       super();
       this.state = { count: 0 }; // State সেট করা হয়েছে
     }

     increment = () => {
       this.setState({ count: this.state.count + 1 }); // State পরিবর্তন করা হচ্ছে
     };

     render() {
       return <button onClick={this.increment}>{this.state.count}</button>; // State থেকে data প্রদর্শন করা হচ্ছে
     }
   }
   ```

// 9. **How can you pass data from one component to another in React?**
   ```javascript
   // Parent কম্পোনেন্ট থেকে child কম্পোনেন্টে props ব্যবহার করে data পাস করা হয়
   function ChildComponent({ message }) {
     return <p>{message}</p>; // পাস করা data প্রদর্শন করা হচ্ছে
   }

   function ParentComponent() {
     return <ChildComponent message="Data passed from Parent!" />; // Props এর মাধ্যমে data পাঠানো হচ্ছে
   }
   ```

// 10. **What is the significance of the `key` prop in lists?**
    ```javascript
    // Key props list items গুলিকে অনন্যভাবে চিহ্নিত করে, যা React-কে পারফরম্যান্স বাড়াতে সাহায্য করে
    const items = ['Apple', 'Banana', 'Cherry'];
    const itemList = items.map((item, index) => <li key={index}>{item}</li>); // Key ব্যবহার করা হয়েছে

    function ItemList() {
      return <ul>{itemList}</ul>; // List rendering
    }
    ```


1. **How can you pass data from one component to another in React?**
   - Data can be passed from one component to another using **props**. The parent component sends data to the child component through props.
   
   **Example:**
   ```javascript
   function ChildComponent({ message }) {
     return <p>{message}</p>; // Data received as props
   }

   function ParentComponent() {
     return <ChildComponent message="Hello from Parent!" />; // Passing data to child
   }
   ```

// 2. **What is the significance of the `key` prop in lists?**
//    - The `key` prop helps React identify which items in the list have changed, been added, or been removed. It is important for optimizing performance when rendering dynamic lists.
   
//    **Example:**
   ```javascript
   const items = ['Apple', 'Banana', 'Cherry'];
   const itemList = items.map((item, index) => <li key={index}>{item}</li>);

   function ItemList() {
     return <ul>{itemList}</ul>; // Key helps React track each list item
   }
   ```

// 3. **How do you handle events in React?**
//    - In React, events are handled using camelCase syntax, and you assign event handlers as functions.
   
//    **Example:**
   ```javascript
   function ClickButton() {
     const handleClick = () => {
       alert("Button clicked!");
     };

     return <button onClick={handleClick}>Click Me</button>;
   }
   ```

// 4. **What are controlled and uncontrolled components in React?**
//    - **Controlled components**: The form elements' value is controlled by React state.
//    - **Uncontrolled components**: The form elements' value is managed by the DOM itself.
   
//    **Example:**
   ```javascript
   // Controlled Component
   function ControlledForm() {
     const [value, setValue] = React.useState('');

     const handleChange = (e) => {
       setValue(e.target.value);
     };

     return <input type="text" value={value} onChange={handleChange} />;
   }

   // Uncontrolled Component
   function UncontrolledForm() {
     const inputRef = React.useRef();

     const handleSubmit = () => {
       alert(inputRef.current.value);
     };

     return (
       <div>
         <input type="text" ref={inputRef} />
         <button onClick={handleSubmit}>Submit</button>
       </div>
     );
   }
   ```

// 5. **What is a React fragment, and why would you use it?**
//    - React fragments allow you to group multiple elements without adding extra nodes to the DOM.
   
//    **Example:**
   ```javascript
   function FragmentExample() {
     return (
       <React.Fragment>
         <h1>Title</h1>
         <p>This is a paragraph</p>
       </React.Fragment>
     );
   }
   ```

// 6. **How do you conditionally render elements in React?**
//    - You can conditionally render elements using JavaScript expressions like `if`, ternary operator (`? :`), or logical AND (`&&`).
   
//    **Example:**
   ```javascript
   function ConditionalRender() {
     const isLoggedIn = true;

     return (
       <div>
         {isLoggedIn ? <p>Welcome back!</p> : <p>Please log in</p>}
       </div>
     );
   }
   ```

// 7. **What are React Hooks? Name a few commonly used hooks.**
//    - React Hooks allow you to use state and other React features in functional components. Commonly used hooks include:
//      - `useState()`: For managing component state.
//      - `useEffect()`: For side effects like fetching data.
//      - `useContext()`: For using React context.
//      - `useRef()`: For referencing DOM elements.
//      - `useCallback()`: For memoizing functions.
//      - `useMemo()`: For memoizing expensive calculations.

//    **Example (useState):**
   ```javascript
   function Counter() {
     const [count, setCount] = React.useState(0);

     const increment = () => setCount(count + 1);

     return (
       <div>
         <p>{count}</p>
         <button onClick={increment}>Increment</button>
       </div>
     );
   }
   ```

// 1. **Explain the `useState` Hook.**
//    - The `useState` hook is used to add state to functional components in React. It returns a pair: the current state value and a function that allows you to update it.
   
//    **Example:**
   ```javascript
   function Counter() {
     const [count, setCount] = React.useState(0);

     const increment = () => setCount(count + 1);

     return (
       <div>
         <p>{count}</p>
         <button onClick={increment}>Increment</button>
       </div>
     );
   }
   ```

// 2. **How does the `useEffect` Hook work?**
//    - The `useEffect` hook allows you to perform side effects (like data fetching, DOM manipulation, etc.) in functional components. It runs after the render and can be configured to run only when specific state or props change.
   
//    **Example:**
   ```javascript
   function FetchData() {
     const [data, setData] = React.useState([]);

     React.useEffect(() => {
       fetch('https://api.example.com/data')
         .then(response => response.json())
         .then(data => setData(data));
     }, []); // Empty dependency array ensures it runs once after the initial render.

     return <ul>{data.map(item => <li key={item.id}>{item.name}</li>)}</ul>;
   }
   ```

// 3. **How can you make an API call in React?**
//    - You can make an API call in React using the `fetch` API or `axios` inside the `useEffect` hook to fetch data when the component mounts.
   
//    **Example (using `fetch`):**
   ```javascript
   function FetchData() {
     const [data, setData] = React.useState([]);
     const [loading, setLoading] = React.useState(true);

     React.useEffect(() => {
       fetch('https://api.example.com/data')
         .then(response => response.json())
         .then(data => {
           setData(data);
           setLoading(false);
         });
     }, []);

     if (loading) return <p>Loading...</p>;
     return <ul>{data.map(item => <li key={item.id}>{item.name}</li>)}</ul>;
   }
   ```

// 4. **What are Higher-Order Components (HOCs)? Give an example.**
//    - A **Higher-Order Component (HOC)** is a function that takes a component and returns a new component with additional props or functionality.
   
//    **Example:**
   ```javascript
   function withLoading(Component) {
     return function WithLoadingComponent({ isLoading, ...props }) {
       if (isLoading) return <p>Loading...</p>;
       return <Component {...props} />;
     };
   }

   function DataDisplay({ data }) {
     return <div>{data}</div>;
   }

   const DataWithLoading = withLoading(DataDisplay);

   // Usage
   <DataWithLoading isLoading={true} data="Some data" />;
   ```

// 5. **Explain the use of React Context API.**
//    - The React Context API allows you to share state across the entire application (or part of it) without passing props down manually through every level of the component tree.
   
//    **Example:**
   ```javascript
   const ThemeContext = React.createContext('light');

   function ThemedComponent() {
     const theme = React.useContext(ThemeContext);
     return <div>{theme === 'dark' ? 'Dark Mode' : 'Light Mode'}</div>;
   }

   function App() {
     return (
       <ThemeContext.Provider value="dark">
         <ThemedComponent />
       </ThemeContext.Provider>
     );
   }
   ```

// 6. **How does the Context API differ from props drilling?**
//    - **Props drilling** refers to passing data from parent to child components through multiple layers, even when intermediate components don't need the data. 
//    - **Context API** allows you to avoid this by providing a way to share data at any level of the component tree, making the code cleaner and easier to maintain.

// 7. **What are refs, and when should you use them in React?**
//    - Refs are used to directly interact with DOM elements in React, such as focusing an input field or measuring an element’s size.
   
//    **Example:**
   ```javascript
   function InputFocus() {
     const inputRef = React.useRef();

     const focusInput = () => {
       inputRef.current.focus(); // Focus the input field
     };

     return (
       <div>
         <input ref={inputRef} type="text" />
         <button onClick={focusInput}>Focus the input</button>
       </div>
     );
   }
   ```

// 8. **What is the `useRef` Hook, and how is it different from `createRef`?**
//    - `useRef` is used in functional components to persist values across renders without causing re-renders when the value changes. `createRef` is used in class components to create references to DOM elements.
   
//    **Example (`useRef` in functional components):**
   ```javascript
   function Timer() {
     const timerRef = React.useRef(0);

     React.useEffect(() => {
       const interval = setInterval(() => {
         timerRef.current += 1;
         console.log(timerRef.current);
       }, 1000);

       return () => clearInterval(interval);
     }, []);

     return <p>Timer: {timerRef.current}</p>;
   }
   ```

// 9. **What is the purpose of `React.memo`?**
//    - `React.memo` is a higher-order component that prevents unnecessary re-renders of a component by memoizing it. It only re-renders when its props change.
   
//    **Example:**
   ```javascript
   const MemoizedComponent = React.memo(function Component({ name }) {
     console.log('Rendering Component');
     return <div>{name}</div>;
   });
   ```

// 10. **Explain React lifecycle methods.**
//     - React lifecycle methods are special methods that get called at different stages of a component’s life. These methods are only available in class components. Common lifecycle methods include:
//       - `componentDidMount()`: Called after the component is mounted.
//       - `componentDidUpdate()`: Called after the component updates.
//       - `componentWillUnmount()`: Called before the component is removed from the DOM.

// 11. **What are `componentDidMount` and `componentWillUnmount`, and when are they used?**
//     - `componentDidMount`: Runs once after the component has been rendered and added to the DOM, often used for initialization or API calls.
//     - `componentWillUnmount`: Runs just before the component is removed from the DOM, used for cleanup like removing event listeners or cancelling API calls.

// 12. **Explain the `useCallback` Hook and why it is useful.**
//     - The `useCallback` hook returns a memoized version of a callback function that only changes if one of its dependencies has changed. It helps to prevent unnecessary re-renders when passing callbacks to child components.
   
//    **Example:**
   ```javascript
   const memoizedCallback = React.useCallback(() => {
     console.log('Callback function');
   }, [dependency]);
   ```

// 13. **How is the `useMemo` Hook used in React?**
//     - The `useMemo` hook is used to memoize expensive calculations so that they are only recalculated when necessary. It returns a memoized value.
   
//    **Example:**
   ```javascript
   const memoizedValue = React.useMemo(() => expensiveCalculation(a, b), [a, b]);
   ```

// 14. **What are controlled components in forms?**
//     - **Controlled components** are form elements where React manages the state. The form input’s value is controlled by React’s state, and changes to the input are handled by React.
   
//    **Example:**
   ```javascript
   function ControlledForm() {
     const [value, setValue] = React.useState('');

     const handleChange = (e) => {
       setValue(e.target.value);
     };

     return <input type="text" value={value} onChange={handleChange} />;
   }
   ```

// These explanations with examples should provide a solid understanding of the key React concepts.